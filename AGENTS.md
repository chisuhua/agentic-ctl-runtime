# PROJECT KNOWLEDGE BASE

**Generated:** 2026-05-11
**Commit:** cc8c7df
**Branch:** main

## OVERVIEW
AgenticDSL 是一个 DSL 执行引擎，通过 Markdown DSL 定义工作流图（DAG），支持 LLM 调用、工具注册、资源管理和预算控制。C++20 实现，使用 llama.cpp 作为 LLM 后端。

## SINGLE-DEVELOPER MODE (单人开发模式)

**项目治理范式**: 本项目为 **单人开发** (Solo Dev), 不存在团队多人协作流程。所有传统团队开发假设均不适用。

### 不适用的传统流程

- ❌ **议程/会议/法定人数** — 没有"评审会议"召集, 不需要 ≥4 人法定人数
- ❌ **表决规则 (一致通过/≥2/3)** — 单人 = 单票, 不需要多票机制
- ❌ **异议角色 (Devil's Advocate)** — 作者 = 评审人 = 实施者, 自审
- ❌ **邮件召集 + Slack 通知** — 双重渠道同步, 不需要
- ❌ **创建日期/签名表** — 单一作者, 不需要合规性元数据
- ❌ **角色 (主席/秘书/记录员)** — 单一作者, 全部由一人承担
- ❌ **Sprint 排期过载的"团队缓冲"** — 单人执行 = 严格串行依赖, 不存在并行人力

### 适用的简化流程

- ✅ **GitHub Issue 作为单一审查入口** — 每个 ADR / 重大变更创建 1 个 issue, 用 issue 评论记录自审决策
- ✅ **Self-Review Checklist** — issue body 内嵌标准化清单 (8-12 项), 自审后勾选
- ✅ **24h Cooling-Off** — issue 创建后至少 24h 才执行, 给"睡一觉再决定"留窗口
- ✅ **OpenSpec Change as Decision Log** — `openspec/changes/` 既是变更记录也是决策证据
- ✅ **Oracle/Metis 作为 Virtual Architect** — 当需要"第二意见"时调用 AI 顾问, 输出写入 ADR 决策段
- ✅ **Atomic Commits** — 每项 ship 自成一 commit, 保留回溯能力

### 治理证据

- ADR-0050 Phase 6 战略评估 ✅ Approved (experimental) — "Solo Dev 重估, Candidate B 服务化"
- capability-application-map §六 "ADR 状态基线交叉验证" — 全部 ADR 由单一作者维护
- 2026-08-25 起: 治理范式正式确立, 取代 2026-08-24 临时启用的"5 议程 + 4 文件"议会式流程

### 关联文档

- `openspec/changes/2026-08-25-sprint-24-pre-launch-self-review/` — Sprint 24 启动前任务安排 (基于 single-developer mode)
- `docs/architecture/capability-application-map-2026-08.md` — 能力/应用地图 (single dev 视角)
- 旧评审材料归档: `docs/architecture/adr-review-minutes/` (251 行议程 + 邮件模板 — 保留作历史参考, 不再用作实际流程)

## SPRINT 24 PRE-LAUNCH (2026-08-25 → Sprint 24 启动周)

**目标**: Sprint 24 启动前完成 6 个 ADR 自审 + capability-map v1.3 更新 + Sprint 24 kickoff issue 创建

**估时总计**: ~4 小时自审 + 24h cooling-off + 30 分钟落地 (跨 2 天)

### 任务清单 (5 步)

| Step | 任务 | 估时 | 产出 |
|---|---|---|---|
| **1** | 创建 6 个 GitHub issue (每个 ADR 一个) | 30 min | 6 issue URLs |
| **2** | 每个 issue body 粘贴 self-review checklist + 跑自审 | 2 h | 6 issue 含决策 (✅/❌/⏸) |
| **3** | 24h cooling-off + 更新 capability-map §七 v1.3 | 30 min | 1 commit |
| **4** | 创建 Sprint 24 kickoff issue (T17 + 4 ADR 启动序列) | 30 min | 1 issue |
| **5** | Sprint 24 启动周执行 T17 骨架 | (Sprint 24) | code |

### 详细任务 (参见 `openspec/changes/2026-08-25-sprint-24-pre-launch-self-review/`)

## STRUCTURE
```
HydraForge/
├── src/
│   ├── core/          # DSLEngine 核心入口
│   │   └── types/     # Context, Node, Budget, Resource 类型定义
│   ├── common/        # 共享组件
│   │   ├── llm/       # LlamaAdapter (llama.cpp 封装)
│   │   ├── tools/     # ToolRegistry (工具注册表)
│   │   └── utils/     # YAML/JSON 解析、模板渲染
│   └── modules/       # 10 个功能模块
│       ├── parser/    # MarkdownParser → ParsedGraph
│       ├── scheduler/ # TopoScheduler (DAG 拓扑调度)
│       ├── executor/  # NodeExecutor (节点执行器)
│       ├── context/   # Context 管理
│       ├── budget/    # BudgetController (预算控制)
│       ├── trace/     # TraceRecord 追踪
│       ├── library/   # StandardLibraryLoader (标准库加载)
│       ├── system/    # System 模块
│       ├── cognitive/ # 认知编排 (SimpleCognitiveOrchestrator, ADR-0019/0020)
│       ├── skill_interpreter/ # Skill 隔离执行 (SkillInterpreter, ADR-0055/0060, --skill-child IPC)
│       └── exports/   # 导出类型定义与设计稿
├── lib/               # DSL 标准库 (.md 文件)
│   ├── auth/          # 认证相关 DSL
│   ├── human/         # 人类交互 DSL
│   ├── math/          # 数学工具 DSL
│   ├── utils/         # 通用工具 DSL
│   └── inference/     # 推理控制面 (engine, model, session 子图)
├── external/          # 第三方依赖 (llama.cpp, nlohmann_json, inja, yaml-cpp)
├── include/           # 公共头文件 (ADR-0019 契约层: contract/cognitive/policy/types/skill)
├── pdk/               # PDK 插件 (loop_agent, fs_tools, shell_tools, provider_agent, llama_engine, model_router)
├── tests/             # Catch2 单元测试 (83 个测试, 零失败)
└── examples/          # 7 个示例程序
    ├── agent_basic/   # ✅ 主要示例：加载 .agent.md 工作流
    ├── agent_simple/  # ✅ 简化示例 (MockLLMProvider 单轮 ReAct)
    ├── agent_loop/    # ✅ 循环执行示例 (MockLLMProvider 多轮)
    ├── slice_01_tool_call/  # ✅ Track 0.2 端到端 (--mock)
    ├── phase1_model_router_plugin/  # ✅ Phase 1 Plugin Stub 模型路由
    ├── phase1_plugin_demo/          # ✅ Phase 1 Plugin Stub ToolResult 演示
    └── pdk_chat_demo/  # ✅ PDK Chat Demo (Agent-as-Plugin, SkillInterpreter, DeepSeek LLM)
```

> **Sprint 19 (2026-06-30) — `examples-mockllm-migration` ship**:`examples/agent_simple/` 和 `examples/agent_loop/` 从 2026-06-13 审计发现的 4 个编译错误债中修复,改用 `MockLLMProvider` + `ILLMProvider` 模式(参考 `slice_01_tool_call` 模板)。`LlamaAdapter`/`InjaTemplateRenderer`/`extract_pathed_blocks` 实际仍存在,无 API 删除冲突;`PromptBuilder` 真删(`9a619f3`,2025-11-05)→ 重写 in-file `build_prompt()` helper;`get_llm_adapter()` 真删 → 改 `get_llm_provider()`。新增根 `CMakeLists.txt` `AGENTICDSL_BUILD_EXAMPLES` opt-in flag(默认 OFF)。

## WHERE TO LOOK
| Task | Location | Notes |
|------|----------|-------|
| 添加新节点类型 | `src/modules/executor/node_executor.h` | execute_xxx 方法 + execute_node 分发 |
| 添加新 DSL 语法 | `src/modules/parser/markdown_parser.h` | create_node_from_json |
| 修改调度逻辑 | `src/modules/scheduler/topo_scheduler.cpp` | build_dag() / schedule() |
| LLM 调用修改 | `src/common/llm/llama_adapter.cpp` | generate() 底层 |
| 工具注册/调用 | `src/common/tools/registry.cpp` | call_tool() / register_tool() |
| 预算管理 | `src/modules/budget/budget_controller.cpp` | ExecutionBudget 扣费 |
| 编写测试 | `tests/test_*.cpp` | Catch2，tag 格式 `[module][stageN]` |
| DSL 标准库 | `lib/*.md` | Markdown 格式的子图定义 |
| Skill 隔离执行 | `src/modules/skill_interpreter/skill_interpreter.cpp` | posix_spawn + IPC (ADR-0055/0060) |
| PDK Agent 循环 | `include/agenticdsl/pdk/agent_loops/` | React / PlanExecue / ForkJoin 三循环 |

## CODE MAP (Key Symbols)

| Symbol | Type | Location | Role |
|--------|------|----------|------|
| DSLEngine | class | src/core/engine.h | 主入口，from_markdown / run |
| ParsedGraph | struct | src/core/types/node.h | 解析后的图结构 |
| TopoScheduler | class | src/modules/scheduler/topo_scheduler.h | DAG 调度器 |
| NodeExecutor | class | src/modules/executor/node_executor.h | 节点执行器 |
| ToolRegistry | class | src/common/tools/registry.h | 工具注册表 |
| LlamaAdapter | class | src/common/llm/llama_adapter.h | llama.cpp 封装 |
| ExecutionBudget | struct | src/core/types/budget.h | 预算结构 |
| LayeredContext | struct | include/agenticdsl/types/layered_context.h | 5-层结构化上下文 (L1-L5, ADR-0008) |
| DomainWorkerPool | class | include/agenticdsl/cognitive/domain_worker_pool.h | 领域智能体工作线程池 (Sprint 3, ADR-0020 §3.2 ✅ Resolved) |
| DomainTask | struct | include/agenticdsl/cognitive/domain_worker_pool.h | 领域任务结构 (domain/tool_name/arguments/output_key) |
| DECLARE_TOOL (macro) | macro | include/agenticdsl/pdk/tool_macros.h | PDK 工具注册宏 (Sprint 4, ADR-0021 🟡 Partial) |
| DEFINE_AGENT (macro) | macro | include/agenticdsl/pdk/agent_macros.h | PDK Agent 循环宏 (React MVP, PlanExecute/ForkJoin Phase 2) |
| SafeExec | class | include/agenticdsl/pdk/safe_exec.h | PDK 沙箱执行封装 (超时+异常 MVP) |
| SkillInterpreter | class | include/agenticdsl/skill/skill_interpreter.h | SKILL.md 隔离执行引擎 (posix_spawn + IPC, ADR-0055/0060, Sprint 22) |
| SessionManager | class | src/core/session_manager.h | JSONL 树状会话存储 (open/fork/branch/compact/build_context/migrate) — 事件发射已 ship (v2, ADR-0068) |
| LlamaEngine plugin | plugin | pdk/llama_engine/ | 首个 PDK 推理引擎 plugin (12 工具: engine/model/arch, C14 Phase 5) |

## CONVENTIONS
- **2 空格缩进**，中文注释（避免中英混杂）
- **命名规范**：CamelCase 类名/结构体，snake_case 变量，SCREAMING_SNAKE_CASE 宏
- **文件头注释**：功能描述、作者、日期
- **CMake**：每个模块独立 CMakeLists.txt，最终聚合成 agenticdsl_core

## ANTI-PATTERNS (THIS PROJECT)
- **禁止** `include_directories()` 全局包含 → 应用 `target_include_directories()`
- **禁止** `link_directories()` → 应在 CMake target_link_libraries 中指定完整路径
- **禁止** `as any` / `@ts-ignore` 类型压制
- **禁止** 空 catch 块 `catch(e) {}`
- **禁止** 删除失败的测试来"通过"

## ENGINEERING PATTERNS (real-llm-core-coverage 沉淀)

**沉淀时间**: 2026-09-08, **来源**: real-llm-core-coverage Phase 0+A+B ship + Oracle 审查 (`SHIPPED: afc2d1b / 117850c / c0cb522`).
**目的**: 把真实 LLM 测试驱动实施中验证过的模式集中记录, 避免后续 Phase C-G 重复踩坑.
**设计原则**: 每条模式配"反模式"对照, 说明什么情境下**不适用**, 防止过度泛化.

### 决策层 (Decision)

#### 1. 测试驱动发现生产 bug 的标准闭环

**触发**: 真实 LLM 测试 FAIL 且根因不在 LLM 输出 (server 报 "you passed X" 等显式错误).

**闭环 4 步**:
1. **诊断定位** — 用 `std::cerr` 打印 captured payload (不要依赖 Catch2 INFO — 并行测试中 INFO 输出可能被吞, 见模式 #5), 精确定位失败帧.
2. **最小修复** — 单点最小变更 (例: `req.params.model.clear()` 一行), 不动架构. 但必须加注释解释"为何这行不是多余的" (未来维护者会误删).
3. **回归守卫** — 加不依赖真实 API key 的 **Recording Provider** 单测 (实现 `ILLMProvider` 三虚, generate 记录 req/payload, 返回固定 JSON), CI skip 下也能拦截回归.
4. **系统性记录** — 列出**所有同类潜伏站点** (grep `GenerationRequest` 默认构造点), 开跟进 change, 避免"逐 phase 红一遍".

**反模式**: 只修一处测试不修系统性 → Phase E/G 必然复现 → 浪费时间.

#### 2. 系统性问题的"触发升级"模式

适用于"一类潜伏问题" (例: `LLMParams` 默认 model 遮蔽 5+ 站点). **不要主观判断** "预感很重要", 定义**量化升级门槛**:
- 例: `fix-generation-request-model-default` 升级条件 = "Phase B-G 暴露 ≥3 站点" → 升为 Phase B 前置 P0.
- 升级条件写在 change 的 tasks.md `## 升级触发` 章节, 达到时直接决策, 避免争论.

**反模式**: "以后遇到再修" → 永远遇到, 永远没时间.

#### 3. 真实 LLM 测试断言强度分层

匹配**测试目的**分层, 不要一刀切:
- **契约验证** (例: A.2 单调用模型名) → 严格 1/1 → flake 是有效信号 (说明契约破了).
- **系统鲁棒性** (例: A.4 5 串行) → 宽松 `≥1 ok + 其余优雅 error + error_message 非空` → 反映 LLM 真实不可控.
- **能力断言** (例: B.4 RateLimited) → 严格 `code == RateLimited` (LLMProvider 层映射是确定的, 与 LLM 输出无关).

**反模式**: 5 个测试都用 `REQUIRE(ok)` → 4/5 flake = 不可用; 或都用 `≥1 ok` → 强契约被弱化.

#### 4. OpenSpec Change 的 SHIP-with-fixes 流程 (Oracle 介入)

**适用**: 跨 ≥3 个文件 / 涉及生产代码修改 / OpenSpec spec 决策需复核的 change.

**流程**:
1. **作者自审** (Single-Dev 模式) → commit baseline.
2. **派 Oracle 审查** (后台) → 收集 SHIP / SHIP-with-fixes / BLOCK 结论 + 修正清单 (按严重度排序).
3. **按清单修正** → 不 amend baseline, **新增独立 commit** (原子性, 回溯能力).
4. **派 Oracle 复核** (续同 session 失败则新开) → 拿 APPROVE 才能 ship.

**反模式**: 
- 不派 Oracle 直接 ship 大 change → 高风险, 一旦发现 bug 需拆 commit.
- 派 Oracle 后一次性修正 + amend → 失去原子性, 无法回溯"原始 ship 时状态".

### 治理层 (Governance)

#### 5. 跨多树相同测试目标的 helper 双维护策略

**触发**: 一个 helper 在 examples 树 (sibling change) 已 ship, core 树也需同能力.

**策略**:
- 项目级 helper 放 `tests/test_helpers/<name>.h` (namespace `agenticdsl::test`), 与 `http_mock_server.h` 一致.
- pdk 副本**保留**为内联副本 ("frozen"), 不修改已 ship sibling 文件.
- 项目级 self-test 独立覆盖相同 4 cases, 验证 API 一致.
- 漂移风险由 duplicated self-test 兜底. 漂移严重时 (≥3 处 API 分歧) 再考虑公共头.

**反模式**: 删 sibling helper 只留项目级 → 破坏 sibling 已 ship 测试 + 失去 frozen reference.

### 工程层 (Engineering)

> 工程层模式沉淀在 `tests/AGENTS.md` (测试目录专属) + `src/common/llm/AGENTS.md` (LLM 模块专属).

### 沉淀源 (Provenance)

- **2026-09-08**: Oracle session `ses_f7f5ef175ffeGKhxXLfBJjzLVX` 审查 + `ses_f330bb6ffehvveECRPGKbPF7` 复核, 模式 1/2/4 直接产出.
- **2026-09-08**: Phase B SIGSEGV 调试 (gdb + 消除实验) 沉淀模式 3 (断言分层) + 异常隔离 §NOTES 扩展.
- **2026-08-04**: chat-real-llm-coverage ship 沉淀 `helper 三态分离` (模式工程层).
- **2026-07-22**: skill-interpreter-real-loading 沉淀 `Recording Provider 守卫` 模式.

## BUILD SYSTEM
- CMake 3.20+，C++20
- 根 `CMakeLists.txt` 聚合 10 个模块静态库 → `agenticdsl_core`
- 构建：`./build.sh` 或 `mkdir build && cd build && cmake .. && make -j$(nproc)`
- 测试：`cmake .. -DAGENTICDSL_BUILD_TESTS=ON && make && ctest --output-on-failure`

## NOTES
- `engine.h` 直接 `#include` 6 个模块头文件 (scheduler/topo_scheduler, parser/markdown_parser, budget/budget_controller, common/llm/llm_types, common/llm/mock_provider, common/tools/registry) — ADR-0019 §1.4 已识别为问题待解, Stage 4 (core-interface-inversion) 处理
- **2026-06-17 更新**: ADR-0019 §1.4 已解决. Sprint 1b (commit `248d209`) 吸收 3 deep modules/ 移除 (topo_scheduler.h / markdown_parser.h / budget_controller.h 通过 PIMPL-lite). 剩余 4 跨模块 include (3 common/ + 1 modules/trace/) 由 OpenSpec change [`2026-06-15-residual-engine-h-decoupling`](openspec/changes/2026-06-15-residual-engine-h-decoupling/) 处理 (P1 active, 估时 3 周). `engine.h` 当前保留 `common/llm/llm_types.h` (types 头文件例外).
- **2026-06-18 更新**: P1.T3 完成 (commit `01666fa`). `engine.h` 第 47 行 `modules/trace/trace_exporter.h` → `agenticdsl/types/trace_record.h` (TraceRecord data-only struct 上移到 include/agenticdsl/types/). 跨模块 include 计数 4 → 3 (仅剩 3 common/). 27/27 测试零回归. 剩余 3 个 common/ include 由 T1 (LLMProviderFactory 从零构建 5-7d) + T2 (IToolRegistry 7 虚函数 5d) 处理, 完成后将达到 ADR-0019 §1.4 完全退出标准 (1 个 modules/common include).
- **2026-06-18 P1 全部 ship (17 commits, T1+T2+T3+T4+T5)**: `engine.h` 跨模块 include 计数 **2→1** (仅 `common/llm/llm_types.h` types 例外). 29/29 测试零回归 (含新增 test_provider_factory + test_tool_registry_interface). ADR-0019 §1.4 状态更新为 ✅ **已解决**. OpenSpec change `2026-06-15-residual-engine-h-decoupling` 准备 archive. 关键实施: IProviderFactory + LLMProviderFactory 路由 (P1.T1) + IToolRegistry 9 虚函数 (P1.T2) + SecureToolRegistry 委托多继承 (P1.T2 v3 修订) + engine.h PIMPL-lite tool_registry_ (P1.T4) + NodeExecutor/TopoScheduler/ExecutionSession ToolRegistry&→IToolRegistry& (P1.T4 依赖倒置).
- **2026-06-30 (Sprint 15 P0-A.0 ship, audit 修正)**: 审计发现 2026-06-30 cross-module include 实际计数 **4 个** (1 types 例外 `common/llm/llm_types.h` + 3 policy 头文件 Sprint 13 ADR-0031 新增: `common/policy/policy_factory.h` + `common/policy/approval_handler.h` + `common/policy/approval_callbacks.h`). Sprint 14 C4 ToolCoordinator 默认注入导致 2 个测试失败 (test_basic / test_no_llm), 改为 opt-in (`DSLEngine::set_tool_coordinator(unique_ptr<ToolCoordinator>)`), 41/41 测试零回归. ADR-0031 §决策 5 ToolCoordinator 设计不变, 仅激活时机从 default-on 改为 explicit.
- **2026-06-19 (Sprint 3 DomainWorkerPool 实施, OpenSpec change `2026-06-30-domain-worker-pool`)**: 新增 `include/agenticdsl/cognitive/domain_worker_pool.h` + `src/modules/cognitive/domain_worker_pool.cpp` (N 个 std::jthread worker + 共享 FIFO 任务队列多消费者模式 + shared_mutex 保护 handler 注册表 + IInteractionBus 集成, 构造双重重载, CP.22 协议 6/6 项通过, 异常隔离 try-catch + catch(...), 队列排空策略). 新增 `tests/test_domain_worker_pool.cpp` 7 个 test case (默认构造 / submit 派发 / 1000x 并发 / 异常隔离 / shutdown 等待 in-flight / graceful vs forced shutdown / bus 集成). 新增 `Dockerfile.tsan` 容器化 TSan 验证 (ubuntu:22.04 + gcc-13 + ASLR=0). 31/31 ctest pass (30 baseline + 1 new test_domain_worker_pool w/ 94 assertions), 零回归. ADR-0020 §2.2.1 状态从 🟡 Partial → ✅ **Resolved**, §3.2 标记为"已实施". OpenSpec change `2026-06-30-domain-worker-pool` 准备 archive.
- **2026-06-19 (Sprint 4 PDK 骨架 实施, OpenSpec change `2026-07-07-pdk-skeleton`)**: 新增 `include/agenticdsl/pdk/{tool_macros,agent_macros,safe_exec,pdk}.h` (DECLARE_TOOL 宏 + DEFINE_AGENT 模板 (React MVP) + SafeExec 封装 (超时+异常 MVP) + PDK 统一入口). 新增 `pdk/CMakeLists.txt` monorepo 子目录 (INTERFACE 库 `hydraforge_pdk`). 根 CMakeLists.txt 添加 `add_subdirectory(pdk)`. 新增 `tests/test_pdk_macros.cpp` 5 个 test case (DECLARE_TOOL 展开 / DEFINE_AGENT 实例化 / SafeExec 超时 / SafeExec 异常 / PDK Runtime 解耦). 32/32 ctest pass (31 baseline + 1 new test_pdk_macros w/ 33 assertions), 零回归. ADR-0021 状态从 🔍 Proposed → 🟡 **Partial**, P3 静态链接验证 (PDK 头文件仅依赖 `agenticdsl/contract/*.h`). OpenSpec change `2026-07-07-pdk-skeleton` 准备 archive. T4b (`hydraforge-pdk` 独立仓库推送) 留 Sprint 4 ship 后异步 (外部阻塞: GitHub 组织存在性).
- **2026-06-19 (Sprint 4 T4b Dual-Repo 治理)**: PDK 采用 Dual-Repo 策略 (Option C: vendored + 单独发布仓库, **非 submodule**). 新增 `scripts/sync-pdk.sh` (~17KB) 自动化同步脚本 (preflight + 文件拷贝 + README 生成 + commit/push + standalone 构建验证). ADR-0021 §7 追加 Dual-Repo Policy (vendored in monorepo `include/agenticdsl/pdk/` + 单独发布仓库 `github.com/chisuhua/hydraforge-pdk`). 触发时机: 每个 Sprint ship 后 / PDK 头文件 API 变更 / 紧急 patch. 路径映射: monorepo `agenticdsl/pdk/` ↔ standalone `hydraforge/pdk/` (API 兼容). 外部消费者通过 `find_package(hydraforge_pdk 0.1 REQUIRED)` 接入. 零内部摩擦 (vs submodule 需要 init), 保留独立版本演进 (sync script).
- **2026-06-18 Sprint 2 CognitiveWorker 实施 (OpenSpec change `2026-06-23-cognitive-worker`)**: 新增 `include/agenticdsl/cognitive/cognitive_worker.h` + `src/modules/cognitive/cognitive_worker.cpp` (per-agent 隔离, 状态机 idle/running/stopped, 构造签名 `(unique_ptr<DSLEngine>, shared_ptr<IInteractionBus>)`, F7 构造时强制注入 bus, TD-CW-02 析构 out-of-line 隐式 stop()+join, TD-CW-03 错误码 bridge 覆盖 9 处 legacy string). 新增 `tests/test_cognitive_worker.cpp` 9 个 test case (生命周期 / 任务提交 / 错误传播 / 并发 10×100 / 状态机 / bridge / F7 / 析构安全). 30/30 ctest pass (29 baseline + 1 new test_cognitive_worker w/ 33 assertions), 零回归. ADR-0020 §2.2.2 新增, §3.1 V3.2 修正, ADR-0019 状态变更日志追加. OpenSpec change `2026-06-23-cognitive-worker` 准备 archive.
- **2026-07-31 (Sprint 19 / decompose-execution-session-h, ship)**: OpenSpec change `2026-06-30-decompose-execution-session-h` 实施完成 — `src/modules/scheduler/execution_session.h` PIMPL-lite 拆分, 6 个 `modules/` include + 1 个同模块 `resource_manager.h` 改用前向声明 + `std::unique_ptr<X>` 间接持有 (ContextEngine/BudgetController/TraceExporter/NodeExecutor). 完整类型移到 `execution_session.cpp`, 析构 out-of-line `= default;` 与 Sprint 18 D.7 (library_loader.h) / Sprint 17 C.4 (topo_scheduler.h) 同模式. 公开 API 零修改: `execute_node()` / `check_and_requeue_dynamic_deps()` / `is_budget_exceeded()` / `get_context_engine()` / `set_approval_handler(IApprovalHandler*)` / `set_tool_coordinator()` 签名不变, `topo_scheduler.cpp` 调用点零变化 (仅添加 `context_engine.h` / `approval_handler.h` / `trace_exporter.h` include 修复 PIMPL 解耦后级联). `execution_session.h` include 计数 14 → 11 (`modules/` 子串 7 → 0, 保留 3 core/types/ + 1 itool_registry + 1 iapproval_handler + 6 标准库). 级联影响: `topo_scheduler.h` + 1 include (`trace_exporter.h` for `get_last_traces()` 内联函数); `topo_scheduler.cpp` + 2 include (`context_engine.h` for `ContextEngine::merge`/`get_snapshot`, `approval_handler.h` for `ApprovalHandler→IApprovalHandler` 隐式转换); `factory.cpp` + 2 include (`resource_manager.h` for `unique_ptr<TopoScheduler>` 销毁, `trace_exporter.h` 同 topo_scheduler); `tests/test_execute_parallel.cpp` + 1 include (`resource_manager.h` 同 factory). 49/49 ctest pass 零回归. ADR-0019 §1.4 推进 (execution_session.h 解耦完成, 全部 PIMPL-lite 已 ship). OpenSpec change archived as `2026-06-30-decompose-execution-session-h`.
- `lib/` 目录存放 `.md` DSL 文件，非 C++ 库
- `src/modules/exports/` 存放导出类型定义
- `src/common/contract/` ADR-0019 契约层 (IInteractionBus, InMemoryBus) — 与 `include/agenticdsl/contract/` 头文件配套
- `llm_config.json` 运行时 LLM 配置（模型路径、温度等）
- `.clang-format` / `.clang-tidy` 存在 (项目根)
- 构建预设: `CMakePresets.json` (cmake --preset debug|release|asan|tsan|tests)
- `compile_commands.json` 根目录软链接 (Stage 5 / Task 23, 指向 `build/compile_commands.json`)
- GitHub Actions CI: `.github/workflows/ci.yml` (Stage 5 / Task 25, 2 presets × 2 compilers matrix)
- **2026-08-12 状态校正**: `Recent Changes` 区中历史 ctest 失败计数（如 `147/150` / `138/141` / `135/138` / `134/136` / `97/98` / `110/111`）为发布当日快照。Wave 2 + Wave 3-A 完整 ship 后所有遗留项已修复。
- **2026-08-25 状态校正 (T14 + T16 ship 后)**: ground truth 升级为 `ctest --output-on-failure` = **184/184 PASS, 0 failures** (cap-map L385 实证 2026-08-25)。T14 行为回归套件 +1 (183) + T16 SLM 路由 +1 = 184。T14 commit `93ebc8d`, T16 commit `c5fb833`，具体修复：fix-markdown-parser-yaml ✅ 2026-08-04, fix-tool-registry-signal-handler-shutdown (mock-mode SIGSEGV) ✅ 2026-08-08, adr-0069-tool-coordinator-hooks ✅ 2026-08-04, session-manager-jsonl ✅ 2026-08-04/05, adr-0070-declare-command ✅ 2026-08-04, fix-loop-agent-bypass ✅ 2026-08-03, fix-markdown-parser-yaml ✅ 2026-08-04, T14 行为回归套件 ✅ 2026-08-25, T16 SLM 路由 ✅ 2026-08-25。

## Recent Changes

- **2026-08-25 (governance/single-dev-mode):** 正式确立 **Single-Developer Mode** 治理范式, 取代临时启用的"5 议程 + 4 文件 + 邮件/Slack"议会式流程. AGENTS.md 加 "Single-Developer Mode" + "Sprint 24 Pre-Launch" 章节. 新增 4 文件: `openspec/changes/2026-08-25-sprint-24-pre-launch-self-review/` (Sprint 24 启动前 5 步任务安排) + `docs/architecture/adr-self-review-checklist.md` (12 项通用 + 4 类专用清单) + `.github/ISSUE_TEMPLATE/adr-review.md` + `.github/ISSUE_TEMPLATE/sprint-kickoff.md` (2 GitHub Issue 模板). 3 个过度设计文件加 ⛛ Superseded 横幅: `adr-review-minutes/adr-0071-0074-distillation-review-2026-08-24.md` (9 议程 + 251 行) + `meeting-notification-template.md` (邮件/Slack 召集) + `meeting-minutes-form.md` (主席填报表 + 表决规则 + 异议角色). **流程转换**: 邮件召集 → GitHub Issue / 召集信 → issue body 模板 / 主席填报表 → Self-Review Checklist / 9 议程会议 → 1 issue self-review + 24h cooling-off. **Oracle session** `ses_fc93a2994ffeyGbFyDJYtP1MhZ` S4 全部 5 项修正应用 + 4 commits 落库 (93ebc8d + c5fb833 + d803158 + 1ca9a32).
- **2026-08-25 (Step 3 cap-map drift fix ship):** `docs/architecture/capability-application-map-2026-08.md` v1.3 → v1.3.1 drift 收口 (commit `c29b2f3`)。OpenSpec change `2026-08-25-cap-map-v1-3-drift-fix` 完整 ship：12 项声明 drift (§七/§八 章节互换 + §六.6 位置 + §二 词汇表 + 标题 + 闭环 1 G15 + §八.5 重复块 + ADR-0071/0074 footer + ctest 184→185 + §一 Sprint 23 + §三 22→23 + 性质标记 + L4 加注) + 4 项 ship 时扩展 (闭环 1+2 G10/G12 stale 同步 + §一标题)。验证：openspec validate OK, adr_lint 80 OK, ctest 185/185 PASS, src/include/pdk/tests/examples 0 变化, archive/ 0 变化。Sprint 24 Step 3 任务完成。
- 2026-08-09 (Wave 3-A / chat-async-io-model-switching, ship FINAL): OpenSpec change `chat-async-io-model-switching` 实施完成 — Wave 3-A Phase C FINAL, `/model <name>` DECLARE_COMMAND for runtime LLM provider/model switching. **Wave 3-A chat-async-io-steering 4-phase 拆分完整 ship**. 关键 ship: (1) **ChatSession API** (`examples/pdk_chat_demo/chat_session.h/.cpp`) — 新 public API `bool request_model_switch(const std::string& provider_name)` + `std::string next_model() const`; `Impl` 新增 `next_model_` + `next_model_mutex_` (std::mutex + std::string, 因 std::atomic<std::string> 不可 trivially copyable); mock-mode guard `if (impl_->provider_mode == "mock" && provider_name != "mock")` 硬编码拒绝 + stderr warning; (2) **command_globals 全局注入** (`commands/command_globals.h/.cpp`) — 新 `ChatSession* g_command_session` 全局 (前向声明 class ChatSession); (3) **main.cpp wiring** (`examples/pdk_chat_demo/main.cpp:440`) — `pdk_chat_demo::g_command_session = &session;` 在 ChatSession 构造后; (4) **model_command.cpp 替换 Wave 1 stub** (`commands/model_command.cpp`) — 移除 `provider_switch_stub` tool 调用, 直接 `g_command_session->request_model_switch(provider_name)`; 处理空参数 → Usage 提示; 处理拒绝 → error 消息; (5) **4 E2E tests** (`examples/pdk_chat_demo/tests/test_model_switching.cpp`) — request_model_switch stores in next_model / request_model_switch rejects non-mock in mock mode (openai/deepseek 拒绝, mock 允许) / request_model_switch rejects empty / next_model returns pending or empty (4 cases, 13 assertions PASS, 含 mock-mode guard stderr 验证); (6) **5 Requirements** in `openspec/specs/chat-async-io-model-switching/spec.md` — /model command via DECLARE_COMMAND / ChatSession request_model_switch API / per-turn model switch at chat() entry / session JSONL persistence / mock-mode hard-rejection (5 added); (7) **1 commit** (`feat(pdk-chat-demo): Wave 3-A Phase C - /model runtime switching`, 9 files +200/-58); (8) **ctest 147/150 PASS** (3 pre-existing 不变: test_pdk_chat_demo_cli_args + test_e2e_real_llm + test_pdk_chat_demo_session_tree_cli_flags, 4 新增 Phase C 测试 PASS, 0 新增回归). **Wave 3-A 完整 ship**:
  - **Phase 0** (fix-tool-registry-signal-handler-shutdown): SIGSEGV fix
  - **Phase A** (chat-async-io-queue-infra): 双队列 + 输入线程
  - **Phase B 7-step wiring** (chat-async-io-cancellation-chain + 4 sub-changes): 7 步 stop_token 链路
  - **Phase C** (chat-async-io-model-switching): /model runtime switching
变更依据: `openspec/changes/archive/2026-08-09-chat-async-io-model-switching/`. **直接解锁** Phase 6 evaluation (ADR-0050 Candidate B - service-ification).
- 2026-08-09 (Wave 3-A / cancellation-chain-step5-e2e, ship): OpenSpec change `cancellation-chain-step5-e2e` 实施完成 — Phase B Step 5 of 5 FINAL, 闭环验证. 关键 ship: (1) **MockBlockingProvider** (`examples/pdk_chat_demo/tests/mock_blocking_provider.h`) — `class MockBlockingProvider : public ILLMProvider`, `generate()` while-loop 检查 `token.stop_requested()` 每 10ms, 返回 `Result::failure(LLMError{Code::Cancelled, ...})` on cancel; `generate_stream()` via `SyncStream` (观察 token, 一次性 emit text); `available_models()` 返回 `mock-blocking-v1`; (2) **5 E2E tests** (`examples/pdk_chat_demo/tests/test_chat_session_cancellation.cpp`) — MockBlockingProvider cancels within 100ms on stop_requested (cross-thread canceller) / MockBlockingProvider returns success when not cancelled within max_block / CancellationRegistry end-to-end token propagation (thread + source + resolution) / Default token never cancels MockBlockingProvider / Token identity preserved through registry resolve_source (5 cases, 13 assertions PASS); (3) **CMakeLists.txt 注册** — `test_chat_session_cancellation` target (PRIVATE link pdk_chat_demo_obj + agenticdsl_core + hydraforge_pdk + Threads); (4) **6 Requirements** in `openspec/specs/cancellation-chain-step5-e2e/spec.md` — MockBlockingProvider observes stop_token / E2E cancellation completes within 100ms / token identity preserved / default token never cancels / cancellation_id resolves to valid token / full regression verification (6 added); (5) **1 commit** (`feat(pdk-chat-demo): Phase B Step 5 FINAL - Mock provider + E2E mid-loop cancellation test`, 4 files +272/-34); (6) **ctest 143/146 PASS** (3 pre-existing 不变, 5 新增测试 PASS, 0 新增回归). **Phase B 7-step wiring 完整 ship**: Step 1+2 (CancellationRegistry+ChatSession) → Step 3 (loop_agent+NodeExecutor+ToolCoordinator) → Step 4 (3 BREAKING loop APIs) → Step 5 (Mock+E2E test). **验证 chat-async-io-steering 提案验收标准**: steering 中断注入 + follow-up 排队接续 E2E PASS + 长 turn 中 stop_token 正确清理 (无泄漏) + ctest 全量零回归 — 全部 PASS. 变更依据: `openspec/changes/archive/2026-08-09-cancellation-chain-step5-e2e/`. **直接解锁** Phase C `chat-async-io-model-switching` (`/model` runtime switching).
- 2026-08-09 (Wave 3-A / cancellation-chain-step4-loop-apis, ship): OpenSpec change `cancellation-chain-step4-loop-apis` 实施完成 — Phase B Step 4 of 5 focused sub-changes. 关键 ship: (1) **3 BREAKING loop APIs** (`include/agenticdsl/pdk/agent_loops/{react_loop,plan_execute_loop,fork_join_loop}.h`) — `LoopResult::run()` 新增 `std::stop_token token = {}` 参数 (default 保持向后兼容, 零调用方修改); (2) **PlanExecuteLoop 内部 2 处替换** — `plan_phase()` + `verify_phase()` 接受 token 参数 + 替换原 `std::stop_token{}` (lines 206, 256) + `run()` entry early-exit check `if (token.stop_requested())`; (3) **ReactLoop early-exit** — `run()` entry check; orchestrator token 转发留待后续 (Phase B scope 控制); (4) **ForkJoinLoop CV wait predicate** — lines 251-257 新增 `token.stop_requested()` 谓词 + 取消时调用 `pool_->stop()` + 取消状态返回 `failed_phase = "Joining"`; (5) **4 Requirements** in `openspec/specs/cancellation-chain-step4-loop-apis/spec.md` — ReactLoop/PlanExecuteLoop/ForkJoinLoop token 参数 + 默认行为保留 (4 added); (6) **1 commit** (`feat(pdk-chat-demo): Phase B Step 4 - 3 BREAKING loop APIs accept std::stop_token`, 4 files +94/-45); (7) **ctest 138/141 PASS** (3 pre-existing 失败不变: test_pdk_chat_demo_cli_args + test_e2e_real_llm + test_pdk_chat_demo_session_tree_cli_flags, 0 新增回归), 0 调用方编译错误 (default 参数零迁移成本). 变更依据: `openspec/changes/archive/2026-08-09-cancellation-chain-step4-loop-apis/`. **修复审计断开点**: #5 (3 loop APIs token 参数). **直接解锁** Step 5 (Mock provider + E2E mid-loop cancel test).
- 2026-08-09 (Wave 3-A / cancellation-chain-step3-loop-agent, ship): OpenSpec change `cancellation-chain-step3-loop-agent` 实施完成 — Phase B Step 3 of 5 focused sub-changes. 关键 ship: (1) **loop_agent 入口** (`pdk/loop_agent/src/pdk_entry.cpp`) — file-static `CancellationRegistry g_loop_registry` + 解析 `loop_args.cancellation_id` + `g_loop_registry.resolve_token(id)` 转发至 Provider bridge (替换原 line 229 强制 `std::stop_token{}`) + early-return check `if (token.stop_requested())` before provider invocation; (2) **NodeExecutor** (`src/modules/executor/node_executor.{h,cpp}`) — `dispatch_to_tool()` 新增 `std::stop_token token = {}` 参数 (default 保持向后兼容) + 转发至 `ToolCoordinator::execute(meta, tool_ctx, args, token)` + YieldNode 路径 (`node_executor.cpp:475`) `generate_stream(req, token)` 改传 real token 而非 `std::stop_token{}`; (3) **ToolCoordinator** (`src/common/tools/tool_coordinator.{h,cpp}`) — `execute()` 新增 `std::stop_token token = {}` 参数 + entry check `if (token.stop_requested())` short-circuit + emit `tool.audit.denied` 事件 (reason=cancelled in `EventBuilder.args()`, trace metadata in `EventBuilder.meta()`); (4) **3 unit tests** (`examples/pdk_chat_demo/tests/test_loop_agent_cancellation.cpp`) — cancellation_id 解析 + token forwarding identity check + ToolCoordinator short-circuit (3 cases, 15 assertions PASS, 含 audit event `payload.data["reason"] == "cancelled"` 验证); (5) **5 Requirements** in `openspec/specs/cancellation-chain-step3-loop-agent/spec.md` — loop_agent parses cancellation_id / provider bridge forwards token / dispatch_to_tool token / YieldNode forwards token / ToolCoordinator short-circuit (5 added); (6) **1 commit** (`feat(pdk-chat-demo): Phase B Step 3 - loop_agent + NodeExecutor + ToolCoordinator cancellation wiring`, 8 files +316/-38); (7) **ctest 138/141 PASS** (3 pre-existing 失败不变: test_pdk_chat_demo_cli_args + test_e2e_real_llm + test_pdk_chat_demo_session_tree_cli_flags, 0 新增回归). 变更依据: `openspec/changes/archive/2026-08-09-cancellation-chain-step3-loop-agent/`. **修复审计断开点**: #3 (loop_agent entry) + #4 (provider bridge) + #6 (NodeExecutor dispatch/yield) + #7 (ToolCoordinator) — 4 处断开点全修复. **直接解锁** Step 4 (3 BREAKING loop APIs token params: ReactLoop/PlanExecuteLoop/ForkJoinLoop).
- 2026-08-09 (Wave 3-A / chat-async-io-cancellation-chain, PARTIAL ship Step 1+2): OpenSpec change `chat-async-io-cancellation-chain` 实施 (partial — 10/64 tasks ship, Step 1+2 only). 关键 ship: (1) **CancellationRegistry** (`examples/pdk_chat_demo/cancellation_registry.h/.cpp`) — `class CancellationRegistry` 持有 `std::unordered_map<string, shared_ptr<stop_source>>`, 4 API (`register_source` 生成 unique id: timestamp_ms + atomic counter / `resolve_token` 取 token / `resolve_source` 取 source for request_stop 传播 / `unregister` 清理); (2) **ChatSession cancellation state** (`chat_session.h/.cpp`) — `Impl` 新增 `cancellation_registry_` + `current_cancellation_id_` 字段; 新 public API `void request_stop()` 通过 `resolve_source` 触发 in-flight cancellation; `chat(input, std::stop_token token = {})` overload 在 entry 时注册 source + 在 `loop_args` JSON 加 `cancellation_id` 字段透传; (3) **4 unit tests** (`tests/test_cancellation_registry.cpp`) — register/resolve round-trip / resolve unknown id 返回 empty / unregister 移除 / resolve_source 返回 shared_ptr (4 cases, 11 assertions PASS); (4) **7 Requirements** in `openspec/specs/chat-async-cancellation-chain/spec.md` — ChatSession 持 stop_source + chat() 接 token / loop/run args 含 cancellation_id / loop_agent 解析 + 转发 token / 3 Loop APIs token 参数 / NodeExecutor 转发 / ToolCoordinator 接 token / E2E mid-loop cancel (7 added); (5) **1 commit** (`feat(pdk-chat-demo): Phase B Step 1+2`, 8 files +540/-25); (6) **ctest 135/138 PASS** (3 pre-existing 不变, 0 新增回归). **⚠️ PARTIAL SCOPE**: 7 步 wiring 仅完成 Step 1+2 (Cancellation Registry + ChatSession state). 剩余 11 tasks 拆为 3 focused sub-changes (per Phase B scope > deep agent 30-min timeout budget): (a) **Step 3** loop_agent + NodeExecutor + ToolCoordinator wiring (Tasks 3, 7, 8); (b) **Step 4** 3 Loop APIs token params (Tasks 4, 5, 6) BREAKING; (c) **Step 5** Mock provider + E2E mid-loop cancel test (Tasks 9-11). 变更依据: `openspec/changes/archive/2026-08-09-chat-async-io-cancellation-chain/` + `.rddf/plans/chat-async-io-cancellation-chain.md`. OpenSpec archived as `2026-08-09-chat-async-io-cancellation-chain` (spec delta `chat-async-cancellation-chain`: +7 added, 7 requirements only — full 12 scenarios deferred).
- 2026-08-08 (Wave 3-A / chat-async-io-queue-infra, ship): OpenSpec change `chat-async-io-queue-infra` 实施完成 — chat-async-io-steering Phase A (queue infrastructure). 关键 ship: (1) **ChatSession::Impl 扩展** (`chat_session.h` + `chat_session.cpp`) — `enum class QueueKind { Steering, FollowUp }` + 6 个 public API (`queue_size(kind)` / `try_clear_queue(kind)` / 2 test helpers `try_push_*_for_test`); (2) **双有界队列** — `std::queue<std::string> steering_queue_ + follow_up_queue_` 各自由独立 `std::mutex` 保护 (避免 contention), 默认 capacity=32 (constexpr `kDefaultQueueCapacity`), overflow 策略: 拒绝新输入 + stderr warning (仅记录长度不记录内容, 防 secret 泄漏); (3) **输入线程分离** — `std::thread input_thread_` 在 `Impl` 构造时启动, 析构时 `stop_input_thread_` atomic flag + `join()`, stdin 读取 + 分类 (`/` 开头 → steering, 否则 → follow-up); (4) **5 Requirements × 10 Scenarios** in `openspec/specs/chat-async-queue-infra/spec.md` — 双队列分离 / overflow 拒绝 + 可观察 / input thread 生产者 + RAII / queue state API / 阻塞约束; (5) **4 新增 sync 路径测试** `examples/pdk_chat_demo/tests/test_chat_session_queues.cpp` — `queue_size reflects enqueue` (2 sections) + `overflow rejects at capacity` + `try_clear returns count` + `input thread joins on destruction`, 4 cases / 41 assertions 全部 PASS; (6) **2 atomic commits** (`3c7f801` propose + `d4fcca1` feat, 5 files +240/-48); (7) **ctest 135/138 PASS** (3 pre-existing 不变: test_pdk_chat_demo_cli_args + test_e2e_real_llm + test_pdk_chat_demo_session_tree_cli_flags, 0 新增回归). 变更依据: `openspec/changes/archive/2026-08-08-chat-async-io-queue-infra/` + `.rddf/plans/chat-async-io-queue-infra.md`. **直接解锁** Phase B `chat-async-io-cancellation-chain` (7 步 stop_token 链路 wiring, queue 基础设施现成).
- 2026-08-08 (Wave 3-A / fix-tool-registry-signal-handler-shutdown, ship): OpenSpec change `fix-tool-registry-signal-handler-shutdown` 实施完成 — chat-async-io-steering 4-phase 拆分 (Phase 0, blocking). 关键 ship: (1) **pre-approval 审计** `docs/audits/2026-08-08-chat-async-io-steering-pre-approval.md` — 8 处 stop_token 链路断开点 (ChatSession 无 token / loop_agent 显式 `std::stop_token{}` 丢弃 / NodeExecutor/ToolCoordinator 无 token 参数 / SIGINT/SIGTERM 走 `std::exit(0)` 绕开停止语义) + ToolRegistry SIGSEGV 根因 (`signal_handler` 直接 `unload_all_plugins()` 绕过 `engine.h:199-205` 成员反向析构保证); (2) **拆分提案** `improvements/chat-async-io-*.md` — Phase 0 (本 change, P0) + Phase A queue-infra (P1) + Phase B cancellation-chain (P0) + Phase C model-switching (P2); (3) **修复** `examples/pdk_chat_demo/main.cpp:65-86, 467-471` — `std::atomic<bool> g_shutdown_requested{false}` 全局 + `signal_handler` 改为单一 atomic store (async-signal-safe) + main loop 循环顶部观察 flag + 走原有 `engine.reset()` → `unload_all_plugins()` 有序路径; (4) **2 子进程回归测试** `examples/pdk_chat_demo/tests/test_signal_shutdown.cpp` — YAML 校验失败场景 + SIGTERM mid-load 场景, fork+exec 子进程捕获 exit code/stderr, 2 cases / 9 assertions 全部 PASS; (5) **`openspec/specs/shutdown-signal-routing/spec.md`** — 4 Requirements (signal 委托 main 线程 / atomic flag async-signal-safe / main loop 观察 flag / 析构顺序保证) × 8 Scenarios 落地; (6) **`openspec/specs/chat-streaming-slash-tui/spec.md` ↗** 无修改 (本次不破坏现有 streaming); (7) **5 atomic commits** (1ca4185 propose + 29a7a06 fix + bc947a0 doc-sync + 4a0c994 archive + 887d660 pre-work docs); (8) **ctest 135/138 PASS** (3 pre-existing 失败不变: test_pdk_chat_demo_cli_args + test_e2e_real_llm + test_pdk_chat_demo_session_tree_cli_flags, 0 新增回归), ASan preset 0 新 error. 变更依据: `openspec/changes/archive/2026-08-08-fix-tool-registry-signal-handler-shutdown/` + `docs/audits/2026-08-08-chat-async-io-steering-pre-approval.md` + `.rddf/plans/fix-tool-registry-signal-handler-shutdown.md`. **直接解锁** Phase A `chat-async-io-queue-infra` (本周末启动) + Phase B `chat-async-io-cancellation-chain` (Phase A ship 后启动).
- 2026-08-07 (Wave 2-B / chat-streaming-slash-tui, ship): OpenSpec change `chat-streaming-slash-tui` 实施完成 — 事件驱动流式渲染 + 系统提示 CLI flags. 关键 ship: (1) **CliOptions 扩展** — `system_prompt` + `append_system_prompt` 2 字段 (9→9 完整 flag 表); (2) **cli_args_parser 扩展** — `--system-prompt TEXT` (overwrite) + `--append-system-prompt TEXT` (append with newline separator) 2 新 flag + 4 新 parser 测试; (3) **ChatConfig::override_system_prompt** — overwrite-then-append 优先级语义 (overwrite 非空则 base, append 非空则追加 `\n`+append) + 4 precedence 测试 (default/overwrite/append/combined); (4) **main.cpp wiring** — 启动 config 加载后调用 `override_system_prompt()`, 长度变化日志 `[main] System prompt overridden (len=N)`; (5) **EventHandler 渲染增强** — `llm.response` 增加 `completion=N` + `ok=true/false` + 错误信息, `loop.decision` 增加 `(tool=X)` 字段, 全事件行加 `[trace=<trace_id>]` 后缀 (保留 topic+trace metadata), **关键修复** EventHandler 订阅合并 `payload.data` + `payload.meta` (之前只读 meta, 导致业务字段丢失); (6) **5 新 EventHandler 渲染测试** (test_event_handler_rendering.cpp) — tokens+ok/ metadata-only-no-null/ decision+trace_id/ budget-alert-interleave/ unknown-field-tolerance. **scope 调整** `loop.token` chunk emission 在 `pdk/loop_agent` 当前未实现 (只 emit loop.turn.start/decision/end), 推迟到 follow-up; **mock-mode SIGSEGV 仍 pre-existing** (fix-markdown-parser-yaml follow-up 跟踪). **ctest** pdk_chat_demo 子集 13/13 新测试 PASS + 0 新回归; 5 pre-existing 失败不变. 12 files changed, +878/-9, 2 commits (`8026984` feat + `717c05a` merge). OpenSpec archived as `2026-08-07-chat-streaming-slash-tui` (spec delta `chat-streaming-slash-tui`: +6 added). 留 follow-up: `fix-markdown-parser-yaml` + `fix-loop-agent-bypass` (加 loop.token emission 到 pdk/loop_agent).
- 2026-08-07 (Wave 2-B / session-tree-tui, ship): OpenSpec change `session-tree-tui` 实施完成 — `--fork <node>` + `--name <name>` CLI 标志落地 + SessionManager::rename_session API. 关键 ship: (1) **CliOptions 扩展** — `fork_node_id` + `session_name` 2 字段; (2) **cli_args_parser 扩展** — `--fork` / `--name` 2 新 flag + 7→7 完整 flag 表 + 5 新 parser 测试; (3) **SessionManager::rename_session(name)** — 写 session_meta JSONL record, 2 rename 测试 PASS + 24 baseline session_manager 测试零回归; (4) **main.cpp 启动顺序改造** — SessionManager 在 engine/loader 前初始化 (fail-fast), `--fork` 路径验证 session_id_to_load + fork 失败 3 种 diagnostic, `--name` 路径验证与 --session 互斥; (5) **StartupCleanupGuard RAII** — 新增匿名 namespace struct, 声明位置 `engine` 后 + `loader` 后, 析构时 `engine.reset()` → `unload_all_plugins()` 修复提前退出路径的 engine/loader 销毁顺序 (Sprint 18-19 PIMPL pattern 同源); (6) **test_pdk_chat_demo_session_tree_cli_flags** (新建, 6 E2E test cases) — `--fork nonexistent_node` / `--fork empty_session` / `--name + --session` / `--fork` 缺值 / `--name` 缺值 / `--help` 含 `--fork`/`--name` 6 场景, 3 PASS + 3 因 pre-existing YAML DSL 校验问题 (fix-markdown-parser-yaml follow-up) FAIL; (7) **test_session_manager_rename** (新建) — 2 rename 测试 PASS. **ctest 134/136** (5 pre-existing 失败: test_pdk_chat_demo_cli_args + test_e2e_real_llm + test_session_tree_commands + test_pdk_chat_demo_session_tree_cli_flags + test_cloud_llm, 0 新增回归). 12 files changed, +853/-4, 2 commits (`39a4323` feat + `2ebe9bf` merge). OpenSpec archived as `2026-08-07-session-tree-tui` (spec delta `session-tree-cli-flags`: +4 added). 留 follow-up: `fix-markdown-parser-yaml` (影响 test_pdk_chat_demo_*_cli_flags 系列测试) + `adr-0070-declare-command` (统一 --fork/--name 到 DECLARE_COMMAND 体系) + `pre-existing SIGSEGV in ToolRegistry::~ToolRegistry()` (测试在 mock 模式启动 + YAML DSL 校验失败时崩溃, 不阻塞 ship 但需独立 follow-up).
- 2026-08-03 (Sprint 22 / promote-event-builder-fulltoolresult-support, ship V2): EventBuilder V2 扩展落地,8 处 operation-result 事件全部迁移,ADR-0068 转 ✅ Approved. 关键 ship: (1) **EventBuilder V2 扩展** (`include/agenticdsl/contract/event_builder.h`) — 全 payload 构造器 `EventBuilder(topic, ToolResult)` 接管 7 字段 (ok/data/meta/error_code/latency_ms/trace_id/metadata) + 5 setter (`.ok(bool)` / `.error_code(ErrorCode)` / `.latency_ms(uint64_t)` / `.trace_id(string)` / `.metadata(json)`); (2) **8 处 operation-result 迁移** — `tool.completed` (node_executor.cpp:196) / `execution.failed` (node_executor.cpp:411) / `cognitive.task.completed` (cognitive_worker.cpp:198) / `domain.task.{completed,failed}` x3 (domain_worker_pool.cpp:226,260,262) / `tool.audit.denied` x2 (tool_coordinator.cpp:220,245) — 全部从 `bus_->emit(BusEvent{...})` → `bus_->emit(EventBuilder(topic, ToolResult).build())`; (3) **新增 `tests/test_event_builder_v2.cpp`** — 9 test cases / 34 assertions 覆盖 7 字段透传 + 5 setter + 链式组合; (4) **ADR-0068 §决策 7 V2 扩展文档** + 附录 A 7 幻影主题全部 ✅ + 状态翻转 🟡 Partial → ✅ Approved; (5) **§5.11 grep 验收返回 0 行**; (6) **6 atomic commits** (event_builder api + test + 8 migration + adr flip + docs sync + archive). **ctest 97/98** (1 pre-existing fail `test_cost_tracking_decorator`, 无关). `tools/adr_lint.py` 64 ADR PASS + `tools/docs_drift_audit.py` 0 DRIFT. OpenSpec archived `2026-08-03-promote-event-builder-fulltoolresult-support`. 完整闭环 adr-0068: Wave 1 partial ship (5 commit `99087f1` + `0fecb54` + `fe24c45` + `3eae4d7` + `3853ef6`) + V2 收官 (6 commits). 留 follow-up: 仍待办 `fix-markdown-parser-yaml` (51 tasks) / `adr-0069-tool-coordinator-hooks` (126 tasks) / `session-manager-jsonl` (68 tasks) / `adr-0070-declare-command` (72 tasks).
- 2026-08-03 (Sprint 22 / adr-0068-event-emission-contract, ship Wave 1 partial): OpenSpec change `adr-0068-event-emission-contract` Wave 1 partial ship — EventBuilder L1 契约层落地 + 5/7 幻影主题强制发射点 + 17 处既有 emit 迁移. 关键 ship: (1) **`include/agenticdsl/contract/event_builder.h`** header-only L1 契约类 (`EventBuilder` 链式 + `.args(json)` schema 必填业务字段 + `.meta(json)` trace 上下文 + `.build()` 自动填 timestamp), 3 种构造方言统一; (2) **§2 TracingDecorator** (新文件 `src/common/llm/tracing_decorator.cpp/.h`) 在 `ILLMProvider::generate()` 前后 emit `llm.request` (model + prompt_hash) / `llm.response` (tokens + duration_ms + error_code?), 错误路径亦 emit; (3) **§3 ToolCoordinator** emit `tool.execution.start` (tool + layer) 在 `call_tool` 入口 + `tool.execution.end` (tool + ok + duration_ms) 在返回点; (4) **§4 ChatSession** emit `session.persisted` (session_id + path) 仅在原子 rename 成功后, 失败路径不 emit (防御性); (5) **§5 NodeExecutor/Cognitive/Domain emit sites** 17 处迁移到 EventBuilder (dsl.call.started/completed, cognitive.task.started, domain.task.started, tool.coordinator.cycle_detected, tool.audit.invoked/completed, llm.token/error/done via stream_to_bus + 3 测试 hand-emit); (6) **§决策 7 Operation-Result vs Telemetry 分类** (ADR 新增章节) — 8 处 raw BusEvent 故意保留 (`tool.completed` / `execution.failed` / `cognitive.task.completed` / `domain.task.{completed,failed}` x3 / `tool.audit.denied` x2), 因 EventBuilder 当前 API 强制 `payload.ok=true` 不能表达 operation-result 业务失败语义, 扩展 API 推迟至 follow-up `promote-event-builder-full-toolresult-support`; (7) **ADR-0068 附录 A 状态翻转** 5 幻影主题 👻→✅ (`llm.request/response` / `tool.execution.start/end` / `session.persisted`), `loop.*` 三主题保持 👻 待 `fix-loop-agent-bypass`; (8) **ADR-0068 状态** 🔍 Proposed → 🟡 Partial (Wave 1 增量 ship 注记); (9) **2 atomic commits** (`99087f1` feat §4 ChatSession + `0fecb54` refactor §5 executor/cognitive/domain). **ctest 110/111** (pre-existing `test_cost_tracking_decorator` 失败与本 change 无关, commit `514c441` Phase 5 引入). `tools/adr_lint.py` 0 errors. `openspec validate --strict` EXIT 0. OpenSpec archived as `2026-08-03-adr-0068-event-emission-contract`. 留 follow-ups: (a) `promote-event-builder-full-toolresult-support` (EventBuilder 扩展 ok/latency_ms/error_code setters + 8 处 raw BusEvent 迁移); (b) §6 E2E mock 重写 (test_event_emission_contract.cpp 新建 + test_e2e_mock.cpp 全面重写); (c) `fix-loop-agent-bypass` 解锁 `loop.turn.*` + `loop.decision` 三主题真实发射.
- 2026-08-01 (Sprint 20 / tf-integration-coverage, ship): OpenSpec change `tf-integration-coverage` 实施完成 — TopoScheduler + Taskflow 集成测试补齐. 关键 ship: (1) **`TopoScheduler::Config::num_workers` 字段** (default 0, 0=hardware_concurrency() 退化,非 0 注入确切 worker 数) + `config_num_workers_` 缓存成员 + `get_parallel_executor_address_for_test()` test-only 访问器; (2) **5 新 contract 验证 case** in `tests/test_execute_parallel.cpp` — 多调用复用 (pointer stability) / 失败注入 (exception containment per Sprint 12 C2 design) / 6 独立节点 dispatch / Worker 注入 (Config{num_workers=2} → max_concurrent ≤ 2); (3) **7 新 advanced case** in `tests/test_execute_parallel_advanced.cpp` (新建) — 100 节点 flat DAG (< 5s) / Fork-Join 4 支同步 / 默认 worker 退化 / 边界: 空 DAG / 1 节点 / 析构安全 / 错误路径; (4) **依赖链派发 case disabled** — `ToolCallNode` 4 参构造不暴露 `metadata` 字段, 推迟至 follow-up (需扩展 Node 构造或提供 helper); (5) **6 atomic commits** (6e2cfc1 propose + c1bd34f test + d0894fc merge + 1481d84 ship + afa98da gate-fix + ef56b47 drift-fix), 4 files changed, +335/-2. **ctest 107/107 ✅ PASS** (96 baseline + 11 active new; 1 disabled), `tools/docs_drift_audit.py` 0 DRIFT, `tools/adr_lint.py` 50 ADR PASS, `openspec validate` passed. 变更依据: `openspec/changes/archive/2026-08-01-tf-integration-coverage/`. 留 follow-up: 依赖边派发 case 需 `Node` metadata 暴露 + TSan/ASan 验证 (plan §5.2-5.3, optional).
- 2026-07-15 (Phase 6 / C19 Spike ship - phase6-service-ification-v1): W3 ship gate §10 11/11 ✅ PASS — C20-Spike complete. 关键 ship: (1) **ADR-0051 finalized** — §决策 Decision 7 (finalized v1 contract) + §不变量 (逻辑非物理隔离 + thread_local 限制) + §观察 (Layer 3 dual memos: 6 consensus + 4 divergent) + §触发条件 (T-1~T-5 escalation triggers) + §后续 (onboarding seed); (2) **ADR-0051 status flipped** 🔍 Proposed → ✅ Approved (experimental); (3) **spike-onboarding.md** created — 6 sections (§1 What IS / §2 What IS NOT / §3 Contract / §4 Decision Tree / §5 Trigger Thresholds / §6 Reference Impls) + RED BANNER; (4) **Layer 3 dual memos** copied to `docs/service-composition/observations/`; (5) **Ship gate verification report** — `docs/audits/2026-07-15-spike-ship-gate-verification.md` (11/11 PASS: Stage Gate / Sprint 23 capacity / ctest 77/77 / ASan documented skip / no forbidden patterns / no ADR amendments / Layer 3 memos / escalation triggers / openspec validate / ToolCoordinator RAII); (6) **OpenSpec archived** — `openspec/changes/archive/2026-07-15-phase6-service-ification-v1/`; (7) **Master plan §十/§十一/§十二 + active-status.md + AGENTS.md synced**; (8) **tasks.md §2-§11 marked complete** (110 checked, 12 deferred: §12 Post-Ship 5 + §13 Promotion Criteria 7); (9) **5 atomic commits** (d6e7762 ADR finalization + d4f0953 onboarding doc + b6effc5 ship gate + 086357c ADR flip + <this> archive+sync); (10) **0 code changes** (W3 全文档变更). §12 (5 Post-Ship items) + §13 (7 Promotion Criteria) deferred to Sprint 24+. C20 kickoff: G2/G4/G5 teams use `docs/service-composition/spike-onboarding.md`. 变更依据: `openspec/changes/archive/2026-07-15-phase6-service-ification-v1/` + master plan `docs/superpowers/plans/2026-07-10-phase5-remainder-adr-sync.md`.
- 2026-07-10 (Sprint 22 / C18 ship - phase5-sprint22-drift-strategic-gate): OpenSpec change `2026-07-10-phase5-sprint22-drift-strategic-gate` 实施完成 — Sprint 22 三 Review Gates 全部完成。关键 ship: (1) **🧭 Architecture Drift Gate ✅ PASSED** — `docs/audits/2026-07-10-drift-gate.md` 4 路检测 (ADR↔code tools + ADR↔docs 跨文档一致性 + active-status↔master plan + code↔docs) 全部 0 CRITICAL / 0 WARNING, 19 ADR Approved 跨 4 个文档状态一致; (2) **🎯 Strategic Alignment Gate 完成** — Oracle session `ses_0ae4b8107ffetONLmb2Sv2wTb5` 评估 4 候选 (A 自进化 8-12 周 / B 服务化 4-6 周 / C 第三方生态 6-8 周 / D Cloud-native 8-12 周), 推荐 **Candidate B (服务化)** — 唯一匹配团队容量 + 兑现 Phase 5 路径投资 + 产出可消费外部接口; (3) **🆕 Stage Gate Evaluation 推迟至 2026-07-18** — `docs/handoff/2026-07-31-stage-gate-evaluation.md` 7 项清单 3 PASS + 3 PARTIAL/PENDING + 1 RISKY, C10/C11/C12 满 2 周稳定期未到; (4) **ADR-0050 Phase 6 战略评估创建** — `docs/adr/adr-0050-phase6-strategic-evaluation.md` 🔍 Proposed, 含 §决策 (推荐 B) + §启动条件 (5 项硬前置) + §不变量风险 (ADR-0020 并发隔离为最危险) + §C19/C20 决策 (C20 激活 / C19 推迟) + §估时 (W1-W6 4-6 周); (5) **Master Plan 同步** — 2 个 plan §十/§十一/§十二 全部追加 C17 + C18 行 + Oracle Phase 6 决议记录; (6) **active-status.md §一 Phase 5 → ✅ 收官 + Phase 6 🟡 待启动** + §六 下一步行动更新; (7) **0 代码变更**, 验证全部通过 (`adr_lint` 30 ADR / `docs_drift_audit` 0 / `check_roadmap_drift` 0 CRITICAL / `openspec validate` exit 0). 变更依据: `openspec/changes/2026-07-10-phase5-sprint22-drift-strategic-gate/` + master plan `docs/superpowers/plans/2026-07-10-phase5-remainder-adr-sync.md`。OpenSpec change 准备 archive。
- 2026-07-10 (Sprint 22 / C17 ship - phase5-adr-states-final-sync): OpenSpec change `2026-07-10-phase5-adr-states-final-sync` 实施完成 — Phase 5 ADR 状态最终同步。关键 ship: (1) **5 个 ADR 状态翻转** 🔍 Proposed → ✅ Approved — ADR-0035 (C14 ship) / ADR-0040 (C14 ship) / ADR-0041 (C16 ship) / ADR-0043 (C13/C14 D3) / ADR-0044 (C14 ship), 每个 ADR 独立 commit + 追加 ship 证据段; (2) **7 个 ADR 排除翻转并文档化** — ADR-0030 V2 (P2 FleetOrchestrator 延迟) / ADR-0037 (因果排序零实施) / ADR-0038 (BatchingQueue 增量决议延迟) / ADR-0039 (JSON 工具未实现) / ADR-0042 (主文档硬性 banner) / ADR-0045 (实施率 ~20%) / ADR-0046 (实施率 ~25%), 排除原因追加到 master plan §十一.3 + active-status.md §四; (3) **文档同步** — `docs/README.md` §adr/ 表格 +5 Approved 行 (覆盖 0035-0046 范围, 之前完全未列出) + `docs/active-status.md` §一 Approved 计数 14 → 18 (+5) + §四 顺延项追加 7 行 + §六 下一步行动更新; (4) **`tools/adr_relationships.py` 重跑** — `docs/adr-management/relationships.md` 重新生成 (5 个状态变化反映); (5) **Metis 审查后范围修正** — 原计划 12 → 5 ADR FLIP (12 ADR 中 7 个存在硬性阻碍, 详 proposal.md §1b); (6) **7 commits** (5 个 ADR 翻转 + 1 个 doc sync + 1 个 relationships 重跑), `openspec validate` exit 0, `tools/adr_lint.py` exit 0, `tools/docs_drift_audit.py` 0 DRIFT items。变更依据: `openspec/changes/2026-07-10-phase5-adr-states-final-sync/` + master plan `docs/superpowers/plans/2026-07-10-phase5-remainder-adr-sync.md`。OpenSpec change 准备 archive。
- 2026-07-10 (Sprint 22 / docs-cleanup-phase-3 ship): 2026-07-10 综合文档对齐审计发现 10 处 drift（🔴 P0×4 / 🟠 P1×3 / 🟡 P2×3），全部 ship。关键 ship: (1) **AGENTS.md:125 C9 日期修正** — `2026-08-03` → `2026-07-03`（与 git commit `a7d22cf` 一致）; (2) **docs/archive/roadmap-status.md Phase 5 过期横幅** — 标注 2026-07-07 快照状态，指向 active-status.md; (3) **docs/active-status.md OpenSpec active 修正** — `1` → `0`（C10-C16 全部 ship + archived，仅 C16 §5 Cloud plugin 顺延至独立 change），§四 阻塞项改名为顺延项; (4) **master plan 头部基线 + §三 状态表刷新** — ctest 65/65→72/72, ASan 61/61→72/72, Phase 5 40%→70%, OpenSpec active 4→0，§三 6 行状态更新（C9/C10/C13/C15/C16 + C14 增注）; (5) **docs/implementation-slices.md 历史快照横幅** — 标注 2026-05-28 最后更新; (6) **docs/phase1-roadmap.md Phase 1 收官横幅** — 2026-06-24 Sprint 5 ship 100%; (7) **ADR-0019 impl-scope 快照脚注** — 说明主文档 🟡 Partial vs impl-scope ✅ Approved 是有意识时点差异; (8) **docs/archive/implementation-roadmap.md:537 ADR-0032 引用措辞修正** — "C9 ship" → "状态提升；C9 docs audit 二次确认"，明确 C9 是 docs-only audit; (9) **ADR-0042 C16 增量决议 banner** — 5 项 C16 已 ship 决策（Decorator/Dual Consumer/available_models pure virtual/PluginLoader V2/LlamaAdapter deprecated）; (10) **drift-10 docs/README.md §specs 横幅** — 已是 no-op（line 79 横幅已正确存在）。**51 insertions(+), 26 deletions(-), 9 files modified**。零代码变更。验证: `openspec validate docs-cleanup-phase-3 --strict` exit 0 + `tools/adr_lint.py` 0 errors + `tools/docs_drift_audit.py` 0 DRIFT items。变更依据: `openspec/changes/docs-cleanup-phase-3/`。OpenSpec change 准备 archive。
- 2026-07-09 (Sprint 21 / C16 ship - phase5-illmprovider-call-chain-v2): ILLMProvider v2 架构落地。关键 ship: (1) **Decorator 链** — CostTrackingDecorator (P0 budget hole 修复) + ComplianceDecorator (hash-only) + RateLimitDecorator (token-bucket), DSLEngine `set_llm_provider()` 也重新包装; (2) **Dual Consumer Model** — OrchestrationILLMProvider 直连 `inference_provider_->generate()`, 7 TC; (3) **`available_models()` pure virtual** — `=0`, 5 个子类 override; (4) **PluginLoader V2** — 5 符号查找 + lifecycle + ABI v2; (5) **ADR 文档** — ADR-0001/0035/0038/0042/0045/0005 修订; (6) **弃用标注** — LlamaAdapter + LlamaAdapterProvider `[[deprecated]]`; (7) **ASan 修复** — `test_execute_parallel` use-after-scope (lambda 引用→值拷贝); (8) **§5 Cloud plugin 顺延** (独立 follow-up). **72/72 ctest 零回归**, ASan 72/72, openspec validate exit 0, sprint-closeout 7/7, openspec archived. 53 files, +3276/-314, 17 new files. Commit: `514c441`, branch: `phase5-inference-orchestration`.
- 2026-07-08 (C14 ship): Phase 5 Llama Engine Plugin (`pdk/llama_engine/`) 实施完成。关键 ship: (1) **pdk/llama_engine/ plugin 骨架** — 12 个推理工具注册 (4 engine: init/generate/stream/status + 4 model: load/unload/list/switch + 4 C13 架构: prefix_cache/kv_cache/decoding/cloud_engine.configure), 遵循 ADR-0034 model_router PDEK plugin 范式; (2) **D5 显式 load_plugin() API** — `DSLEngine::load_plugin()` 公开方法, 构造时**不**自动加载 (与 C7 一致模式); (3) **D1 SamplerStrategy 删除** — 采样器 clamp 逻辑内联到 generate/decoding 工具; (4) **D3 工具命名统一 inference.*** — engine/model 8 工具使用 `inference/engine/*` / `inference/model/*`; (5) **C13 架构工具** — 4 个配置工具 (prefix_cache/kv_cache/decoding.configure + cloud_engine PLACEHOLDER); (6) **`tests/test_llama_engine_plugin.cpp`** — **10 test cases 全部 PASS** (plugin 生命周期 / 12 工具注册 / 架构工具 5 种 sampler / metadata 审批策略); (7) **lib/inference/engine.md + model.md** — 从 PLACEHOLDER 升级为真实 schema, 引用对应 inference.* 工具. **65/65 ctest 零回归** (64 baseline + 1 新 test_llama_engine_plugin w/ 10 cases, 16 assertions). 变更依据: `openspec/changes/phase5-llama-engine-plugin/` + `docs/adversarial-reviews/decisions-2026-07-07.md`.
- 2026-07-03 (Sprint 19 / LSP discipline ship - lsp-false-positive-fix): Strategic Alignment Gate §9.4 准备期间发现 pdk/model_router/ 4 个文件 LSP 报 8 个 namespace false positive 错误, 根因为 LSP 客户端缓存陈旧 + 缺少 .clangd 配置。关键 ship: (1) **`.clangd` 配置文件** — 项目根新增 .clangd (Diagnostics.Suppress unused_result/unused-lambda-capture + Index.Background Build + StandardLibrary Yes), 让 LSP 配置显式化, 避免依赖 clangd default; (2) **`scripts/check-lsp-discipline.sh`** — 新增 4 项 LSP discipline 检查工具 (compile_commands.json symlink 有效性 / .clangd 格式 / clangd --check 关键文件 0 errors / pdk coverage ≥4 entries), 退出码语义化 (0=pass, 1=config issue, 2=false positive, 3=real LSP error), `--quick` 模式 5s, 完整模式 ~30s/文件; (3) **`scripts/sprint-closeout.sh` Step 6/7** — Sprint 收官时自动跑 LSP discipline 验证, false positive 警告 + 真实错误失败; (4) **Drift 修复** — C10/C11/C12 proposal.md 引用 archive C9 完整名 `2026-07-03-2026-07-03-phase4-5-impl-scope-audit` (双日期前缀), 消除 PLACEHOLDER_DEP_MISSING drift; (5) **审计文档** — `docs/audits/2026-07-03-pdk-model-router-lsp-false-positive.md` 记录根因 + 修复 + 预防机制. **61/61 ctest 零回归** (零代码逻辑变更). 验证: clangd --check 4 pdk 文件 "0 errors", sprint-closeout 7/7 步全绿, 8s. 设计依据: `docs/audits/2026-07-03-pdk-model-router-lsp-false-positive.md`.
- 2026-07-03 (Sprint 19 / C9 ship - adr-impl-scope-audit): OpenSpec change `docs-code-drift-audit-2026-06` 实施完成 — ADR 实施范围审计。关键 ship: (1) **11 个 impl-scope audit 文档** — `docs/adr/adr-*-impl-scope.md` 系列完成, 包含所有 ADR 的实施范围分类 (Shipped/Evolved/Deferred); (2) **文档同步** — `docs/archive/roadmap-status.md` Phase 4.5 → Phase 5 过渡标记为 100%; (3) **AGENTS.md 更新** — 添加 C9 ship 记录与 ADR 状态校准确认; (4) **ADR 状态保持** — 所有 11 个 ADR 保持现有状态 (✅ Approved 或 🟡 Partial), 未发现需要状态变更的核心契约缺失. **54/54 ctest 零回归** (仅文档变更). 变更依据: `openspec/changes/docs-code-drift-audit-2026-06/`。Phase 4.5 全部 100% → Phase 5 自举待启动。
- 2026-07-03 (Sprint 18 / C8 ship - phase-4-5-mvp-cleanup): OpenSpec change `2026-06-26-phase-4-5-mvp-cleanup` 实施完成 — Phase 4.5 MVP 清理。关键 ship: (1) **SimpleCognitiveOrchestrator @internal 标记** — `include/agenticdsl/cognitive/simple_orchestrator.h` + `src/modules/cognitive/simple_orchestrator.cpp` 注释更新, 删除 2 处 `TODO(mvp)` 标记, 标记为 "@internal Phase 0 单轮 ReAct 编排器 — Phase 1+ 由 CognitiveWorker + ReactLoop 封装"; (2) **examples/ 目录梳理** — 新建 `examples/README.md` (8 个 entry 说明: 6 C++ 编译示例 `agent_basic/agent_simple/agent_loop/slice_01_tool_call/phase1_model_router_plugin/phase1_plugin_demo` + 2 参考文档 `skill_porting/superpowers`), 零删除; (3) **文档同步** — `docs/archive/roadmap-status.md` Phase 4 + 4.5 → 100%. **52/52 ctest 零回归** (仅注释+文档变更). 变更依据: `openspec/changes/2026-06-26-phase-4-5-mvp-cleanup/`。Phase 0-4.5 全部 100% → Phase 5 自举待启动。
- 2026-07-02 (Sprint 16 / C6 ship - adr-0004-v2-metadata-approval): OpenSpec change `2026-06-26-adr-0004-v2-metadata-approval` 实施完成 — ToolRegistry V2 安全模型升级。关键 ship: (1) **IToolRegistry 接口升级** — `register_tool_function(name, ToolMetadata meta, ToolFunc fn)` 签名 BREAKING, `register_tool<Func>(name, meta, func)` 模板同步升级; (2) **ToolRegistry 注册时 validation** — 名称非空/重复检查 + category×approval 冲突检查 (dangerous 类目不允许 plan+agent 都关闭审批) + min_layer×allowed_layers 一致性 + Layer×Category 矩阵合法性; (3) **SecureToolRegistry 委托更新** — `register_tool_function` override 同步加 ToolMetadata 透传; (4) **DECLARE_TOOL 宏升级** — 从 2 参数 (name, desc, body) → 4 参数 (name, desc, category, approval_policy, body), 强制要求 ToolCategory + ApprovalPolicy; 新增 `make_approval()` 字符串→ApprovalPolicy 转换 helper (`"always"` → force_approval_always=true, `"yolo"` → only yolo, `"plan"` → only plan, `"agent"` → plan+agent), 宏展开自动生成 ToolSpec.metadata 字段; (5) **Layer×Category 注册时矩阵** — `check_registration_permission(ToolMetadata)` 验证 allowed_layers 合法性 (Cognitive 只允许 ReadOnly, Thinking 允许 ReadOnly+WriteFile, Workflow 全部允许); (6) **ToolPreview.metadata_json 桥接** — ToolCoordinator 在 `ApprovalHandler::process_request()` 前构造 `preview.metadata_json` (ToolMetadata V2 完整 JSON), TUI `/apply` 可渲染; (7) **PDK include 路径调整** — `pdk/CMakeLists.txt` 添加 `src/` 到 target_include_directories 使 `common/policy/execution_policy.h` 可解析; (8) **9 处调用点迁移** — `secure_tool_registry.cpp` + 4 examples (agent_basic/agent_simple/slice_01_tool_call) + 7 test files (test_engine_factory/test_execute_parallel/test_executor/test_no_llm/test_scheduler/test_secure_tool_registry/test_simple_orchestrator/test_tool_registry_interface) + `DSLEngine::register_tool` 模板. **54/54 ctest 零回归**, `make -j$(nproc)` 零编译错误, BREAKING 修复 (test_no_llm 旧重复注册 calculate → 依赖默认 calculate). ADR-0004 状态 🟡 Partial → ✅ Approved (V2). 变更依据: `openspec/changes/2026-06-26-adr-0004-v2-metadata-approval/`。
- 2026-07-02 (Sprint 17 / C7 fully shipped - adr-0034-model-router-plugin): OpenSpec change `2026-06-26-adr-0034-model-router-plugin` Phase 1+2 完整 ship — IModelRouter 首个真实 PDK Plugin 示例。关键 ship: (1) **3 个路由策略 .so** — `CostModelRouterPolicy` (最低 per_token_cost) + `QualityModelRouterPolicy` (tag 匹配度排序, 0 匹配 fallback candidates[0], empty tags 按 n_ctx+max_tokens 总分) + `LatencyModelRouterPolicy` (最低 avg_latency_ms, budget_remaining 字段复用为 max_latency); (2) **ModelRegistry 工具 .so** — `model_router/registry` 注册 `model_router/{cost,quality,latency,registry}` 4 个工具, lambda 解析 args JSON; (3) **每个 .so 导出 `pdk_plugin_info`** — PluginLoader ABI 兼容性检查需要; (4) **examples/phase1_model_router_plugin 升级** — Sprint 0 stub → PluginLoader 演示 (加载 4 .so, --mock 演示 3 策略 + MockLLMProvider, --list 演示 registry), 修复 destruction order 关键 bug (PluginLoader 必须晚于 ToolRegistry 声明); (5) **11 个新 TEST_CASE** — `test_model_router_policy.cpp` 追加 8 (quality 4 + latency 4) + 新建 `test_model_router_registry.cpp` (3: list-all/filter-by-tag/no-match); (6) **61/61 ctest 零回归**, openspec validate exit 0. ADR-0034 🔍 Proposed → ✅ Approved. 变更依据: `openspec/changes/2026-06-26-adr-0034-model-router-plugin/` + `docs/superpowers/plans/2026-07-02-c7-phase2-model-router-plugin.md`。
- 2026-07-02 (Sprint 15 / C5 ship - adr-0033-session-hierarchy): OpenSpec change `2026-06-26-adr-0033-session-hierarchy` 实施完成 — 三层会话模型完整实现。关键 ship: (1) **新建 `src/core/types/session.h` + `.cpp`** — UserSession (顶层, deque<TaskSession> + vector<ToolResult> messages + current_task_session_ pointer), TaskSession (deque<SubtaskSession> + shared_ptr<IExecutionPolicy> + failure_count_ + record_failure/determine_failure_mode), SubtaskSession (POD-like struct, fork/join 最小执行单元); (2) **容器采用 `std::deque`** (非 vector) 确保地址稳定性 (Metis F1/F2), UserSession::task_sessions_ 和 TaskSession::subtask_sessions_ 均用 deque; (3) **TaskSession 持有 `shared_ptr<IExecutionPolicy>`** (Oracle R3, 与 DSLEngine::policy_ 共享); (4) **`DSLEngine::run(UserSession&, const string&, Context)` + `run(UserSession&, const string&, LayeredContext)`** 两个会话感知重载, message → ctx["user_input"], completed 状态复用 TaskSession (多轮对话), failed 状态自动分裂; (5) **failure_count_ 仅可重试错误递增** (Oracle R6, is_retryable_error: Retry/Timeout/ResourceExhausted), <3→KeepSession, ≥3→NewSession; (6) **TopoScheduler 签名完全不变** (Oracle R4), SubtaskSession 创建/归档在 DSLEngine::run_impl() 层; (7) **7 个新测试** — `tests/test_session.cpp` (5 unit: 创建与层级/Subtask归档/失败分裂/IPER retry复用/messages保护) + `tests/test_dslengine_session.cpp` (2 integration: DSLEngine session 重载/多轮复用+LayeredContext 桥接); (8) **54/54 ctest 零回归** (52 baseline + 2 新测试文件, 7 TEST_CASE). 变更依据: `openspec/changes/2026-06-26-adr-0033-session-hierarchy/`。
- 2026-08-01 (Sprint 20 / pdk-plan-execute-fork-join, ship): OpenSpec change `pdk-plan-execute-fork-join` 实施完成 — PDK 扩展 3 种 Agent 循环全部支持。关键 ship: (1) **新增 `include/agenticdsl/pdk/agent_loops/` 子目录 + 4 个头文件** — `loop_result.h` (统一返回值 5 字段: success/message/final_context/retries_used/total_steps/failed_phase) + `react_loop.h` (Sprint 4 React MVP 升级为独立 class) + `plan_execute_loop.h` (Sprint 20 新增, 5 状态机 Planning→Executing→Verifying→Done/Retry, mock LLM 集成 + 3 retry 机制) + `fork_join_loop.h` (Sprint 20 新增, 4 状态机 Forking→Executing→Joining→Done, 复用 DomainWorkerPool Sprint 3 并发能力 + IInteractionBus Sprint 2 事件同步); (2) **修改 `agent_macros.h`** — 移除 Sprint 4 `static_assert(loop_type == React)` 限制, 引入 `LoopDispatcher<AgentLoopType>` 主模板 + 3 specialization + `AgentRunner<AgentLoopType>` 模板 + DEFINE_AGENT 宏改用 `LoopDispatcher<LoopType>::Type`, 3 种 LoopType 全部支持 (BREAKING: DEFINE_AGENT.run() 返回类型从 ToolResult 统一为 LoopResult, React 路径通过 ReactLoop 内部 SimpleCognitiveOrchestrator 委托保持行为); (3) **`tests/test_pdk_plan_execute.cpp` + `tests/test_pdk_fork_join.cpp`** — 10 个新测试 (5+5), file(GLOB) 自动注册, 51/51 ctest 零回归, 15/15 PDK 测试 PASS (5 React + 5 PlanExecute + 5 ForkJoin); (4) **ADR-0021 状态同步** — 状态保持 ✅ Approved + Sprint 20 增量 ship 注记。变更依据: `openspec/changes/pdk-plan-execute-fork-join/`。
- 2026-08-01 (Sprint 20 / migrate-context-to-layered, ship): OpenSpec change `migrate-context-to-layered` 实施完成 — Context → LayeredContext (5-层结构化, ADR-0008) 桥接期 ship。关键 ship: (1) **`include/agenticdsl/types/context_flatten.h`** 新增 `agenticdsl::to_context(flat)` + `agenticdsl::from_context(lc)` 双向桥接 (`inline` 0 开销, `from_context` 复用 `flatten_layers`); (2) **`src/core/engine.h/.cpp`** 新增 `ExecutionResult run(const LayeredContext&)` 公开方法, 旧 `run(const Context&)` 标记 `[[deprecated]]` 1 行委托 `return run(to_context(context))`, 新方法透传 `ctx.working` 给 scheduler 保持向后兼容; (3) **`src/core/types/node.h`** 新增 `virtual LayeredContext execute(LayeredContext&) = 0` + 旧签名标记 `[[deprecated]]` + 基类默认实现 `inline Node::execute(Context&)` 委托 (3 行: to_context → 子类 execute → result.working); (4) **`src/modules/executor/node.cpp`** 6+ 节点子类 (Start/End/Assign/DSL/ToolCall/Resource/Fork/Join/GenerateSubgraph/Assert) 全部实现新 `LayeredContext execute(LayeredContext&)` 签名, 实现体 `return ctx;` (实际逻辑仍在 NodeExecutor); (5) **测试 fixture 迁移** — `test_basic.cpp` (2 处) + `test_no_llm.cpp` (3 处) + `test_engine.cpp` (1 处) + `test_executor_with_mock_provider.cpp` (1 处) 改用 `LayeredContext` + `ctx.working[...]`; (6) **`tests/test_context_adapter.cpp`** 5 新测试 (34 assertions) — empty/nested/array/path access/inja template 5 case 全部 PASS; (7) **52/52 ctest 零回归** (47 baseline + 5 新桥接), 0 deprecation warning 残留。变更依据: `openspec/changes/migrate-context-to-layered/`。OpenSpec change 准备 archive。
- 2026-07-01 (Sprint 20 / extract-http-mock-server-helper, ship): OpenSpec change `extract-http-mock-server-helper` 实施完成 — 提取 `HttpMockServer` RAII helper 消除 `tests/test_http_adapter.cpp` 4 处 httplib server 启动/关闭模板重复 (审计 D-10 P3 测试可读性). 关键 ship: (1) **新建 `tests/test_helpers/http_mock_server.h`** (63 行, namespace `agenticdsl::test`) — 公开 API `HttpMockServer()` 构造 + `~HttpMockServer()` 自动 stop/join + `port() const` + `server()` 引用, 禁用拷贝 (server 句柄不可复制), 绑定失败抛 `std::runtime_error`; (2) **重构 `test_http_adapter.cpp`** 4 TEST_CASE (200 OK / 404 / 500 / SSE chunk) 使用 `HttpMockServer mock;` + `mock.server().Post(...)` + `mock.port()` 替代手写 `httplib::Server svr` 模板; (3) **CMake** — `file(GLOB test_*.cpp)` 自动从 `tests/` 找到, header include path `${CATCH_INCLUDE_DIR}` = `tests/` 解析 `#include "test_helpers/http_mock_server.h"` ✓ 0 CMake 变更. ctest: `test_http_adapter` **7 test cases / 13 assertions PASS** (注: 16→13 因 3 个 `REQUIRE(port > 0)` 移入 helper 改 throw, 行为等价; pre-existing 4 失败在 test_basic/test_engine/test_executor_with_mock_provider/test_no_llm 与本 change 无关, 来自并行 `pimpl-node-executor-h`/`migrate-context-to-layered`). 文件: `tests/test_http_adapter.cpp` 181→149 行 (-32 net, 0 重复模板); helper +63 行 (含 Doxygen). 整体 +31 (spec 预测 -25, 偏差因 helper 含详细 Doxygen 注释). 4 SPEC 验收场景全部满足: Helper 类定义 / 绑定失败抛异常 / 自动清理 / 4 处替换 / 行为完全保持 (port>0 改 throw) / 零回归 (本 change 范围内). OpenSpec change 准备 archive.
- 2026-08-01 (Sprint 21 / loop-agent-dsl-execution, ship): OpenSpec change `loop-agent-dsl-execution` 实施完成 — Loop Agent DSL 执行管道完整落地。关键 ship: (1) **子引擎 LLM provider 传播** — `DSLEengine` 新增子引擎 provider 透传机制, Loop Agent `.agent.md` 中的 LLM 调用通过父引擎 provider 执行; (2) **DSL 文件存在性测试** — `tests/test_loop_agent_plugin.cpp` 新增 file-not-found 错误路径 + DSL 文件存在性验证; (3) **CostTrackingDecoator 并发隔离测试** — `tests/test_engine.cpp` 新增 decorator 链并发安全验证; (4) **tasks.md 同步** — 标记 Gap 1 完成, 标注 deferred items. **83/83 ctest 零回归**. 变更依据: `openspec/changes/archive/2026-07-20-loop-agent-dsl-execution/`。
- 2026-07-22 (Sprint 22 / skill-interpreter-real-loading, ship): OpenSpec change `skill-interpreter-real-loading` 实施完成 — ADR-0055 SKILL.md 执行隔离模型落地。关键 ship: (1) **SkillInterpreter PIMPL 类** — `include/agenticdsl/skill/skill_interpreter.h` + `src/modules/skill_interpreter/skill_interpreter.cpp` (posix_spawn + execve(/proc/self/exe, --skill-child) + seccomp(BPF) + pipe IPC); (2) **skill_child_main 子进程入口** — `src/modules/skill_interpreter/skill_child_main.cpp` (4 host function: call_tool / emit_event / consume_budget / llm_generate); (3) **ADR-0055 正式审批** — 4 处修订: fork→posix_spawn、V1 硬编码 default_skill_capability、V1 defer host_read_context、6 新增 ErrorCode; (4) **Spike §0.4 验证** — 5 CRITICAL 风险评审 (3 PASS + 1 FAIL fixed in §1.3 + 1 documented); (5) **18/18 ctest 零回归** (含新增 test_skill_interpreter). 变更依据: `openspec/changes/archive/2026-07-22-skill-interpreter-real-loading/` + `docs/audits/2026-07-21-skill-interpreter-spike-0.4.md`。
- 2026-07-18 (Sprint 21 / pdk-chat-demo-runtime-fixes, ship): OpenSpec change `pdk-chat-demo-runtime-fixes` 实施完成 — PDK Chat Demo 端到端验证通过。关键 ship: (1) **DeepSeek cloud LLM provider 集成** — 新增 `src/common/llm/deepseek_provider.cpp` + config.json API key 管理; (2) **provider key 不匹配修复** — agent config 中的 provider 字符串 → `LLMProviderFactory` 路由不匹配; (3) **mutex 死锁修复** — `OrchestrationILLMProvider` + `CostTrackingDecorator` 间接递归锁; (4) **shutdown SIGSEGV 修复** — InMemoryBus dispatch_thread `join()` 时序; (5) **examples/pdk_chat_demo/skills/ 目录** — SKILL.md 示例 fork. 变更依据: `openspec/changes/archive/2026-07-18-pdk-chat-demo-runtime-fixes/`。
- 2026-07-31 (Sprint 19 / pimpl-node-executor-h, IN PROGRESS): OpenSpec change `pimpl-node-executor-h` 实施完成 — 引入 `IApprovalHandler` 抽象接口解耦 `NodeExecutor` 对具体 `ApprovalHandler` 的依赖. 关键 ship: (1) **新建 `include/agenticdsl/policy/iapproval_handler.h`** — 1 虚函数抽象接口 (`process_request(meta, ctx, preview)`), 复用 `iexecution_policy.h` 值类型 (`ToolMetadata`/`ToolCallContext`/`ToolPreview`); (2) **`ApprovalHandler : public IApprovalHandler`** — `process_request` 添加 `override`, 公开 API 完全保持; (3) **`node_executor.h` 解耦** — 移除 `#include "common/policy/approval_handler.h"`, 改 `#include "agenticdsl/policy/iapproval_handler.h"`, 成员 `ApprovalHandler*` → `IApprovalHandler*`, `set_approval_handler(ApprovalHandler*)` → `set_approval_handler(IApprovalHandler*)` (BREAKING); (4) **级联消费者对齐** — `execution_session.h` / `topo_scheduler.h` / `factory.h` 的 `ApprovalHandler*` 字段与签名同步迁移到 `IApprovalHandler*` (与 Sprint 18 P1.T4 模式一致). 已知状态: 模块级编译验证 `agenticdsl_modules_executor` + `agenticdsl_common` PASS; 完整 ship gate 阻塞于 `agenticdsl_modules_scheduler` 因并发 `decompose-execution-session-h` agent 的 PIMPL refactor `get_trace_exporter() → const TraceExporter&` 需要完整类型 (非本 change 责任, 建议该 agent 收尾后重跑 ctest). 与 P1.T4 (ToolRegistry → IToolRegistry) 一致遵循 ADR-0019 §1.4 + ADR-0031 §决策 5.
- 2026-07-15 (Sprint 18 ship): OpenSpec change `reduce-topo-scheduler-complexity` ship (5 commits: e57bd35 + 1f309bc + 6dd5f68 + 821ef2f + 586ae27). 关键 ship: (1) **D-1 删除 build_dag(DagState&) 11 行冗重重载** (commit `e57bd35`, BREAKING) — execute/execute_parallel/rebuild_dynamic_graph 3 处调用改为显式 7 行 state 迁移; (2) **D-2 build_dag() 88 → 6 行** (commit `1f309bc`) — 提取 parse_node_wait_for_deps() (in_degree/reverse_edges/wait_for + next/wait_for 解析) + seed_initial_ready_queue() (in_degree==0 节点入队) 2 helpers; (3) **D-3 load_graphs 拆分** (commit `6dd5f68`) — 提取 parse_single_node_spec() helper (注: 实际 JSON→Node 解析在 MarkdownParser::create_node_from_json, helper 签名适配 Node& 而非 json&); (4) **D-4 execute_parallel 56 → 19 行** (commit `821ef2f`) — 提取 execute_dag_loop() 包含 tf::Taskflow 构建 + precedence + run + finalize; (5) **D-5/D-5.1 DRY + 主循环提取** (commit `586ae27`) — copy_dag_state_to() 复用 3 处重复 7 行迁移; run_main_loop() 从 execute() 提取 47 行主 while 循环, execute() 56 → 9 行. 全部 10 函数 ≤50 行. ctest: **49/49 PASS** 零回归. 文件 695→701 行 (+6 net, 2 新 helpers); 控制流 193→186 (-7); 单函数平均控制流 << 15. 偏离: 文件级控制流 186 vs spec <100 — 需要更深度抽象, 留 Sprint 19 follow-up. OpenSpec change 准备 archive.
- 2026-06-30 (Sprint 18 ship): OpenSpec change `fix-audit-quick-debt-2026-06` ship (5 commits: 0df99b3 + e725381 + e9e21f9 + 289d4b6 + a05ccaf). 关键 ship: (1) **D-3 修复 resource.h 循环 include** (commit `e725381`) — 删除未使用的 `node.h` + `context.h` 行 + 错误注释; (2) **D-4 移除 ToolResult::error(string) @deprecated 重载** (commit `e9e21f9`, BREAKING) — 全代码库 10 处字符串调用点迁移 ErrorCode enum, 新增 `llm_error_to_error_code()` 桥接 (NETWORK/RATE_LIMITED/SERVER_ERROR → Retry, AUTH → PermissionDenied, CONTEXT_OVERFLOW → ResourceExhausted), 删除 dead `llm_error_code_name()`, 4 处 test assertion 同步更新, BREAKING 标注; (3) **D-7 PIMPL-lite MarkdownParser 解耦** (commit `289d4b6`) — 前向声明 + unique_ptr + 析构/构造 out-of-line, 与 Sprint 17 C.4 同模式; (4) **D-11 ADR-0032 状态同步** (commit `a05ccaf`) — 🟡 Partial → ✅ Approved, 反映 `tests/test_cost_collector.cpp` (2026-06-14, commit `451e395`) 已 ship; (5) OpenSpec 跟踪 cleanup (commit `0df99b3`) — git-detected rename `openspec/changes/audit-quick-wins/.openspec.yaml → fix-audit-quick-debt-2026-06/.openspec.yaml` (50% 相似度). ctest: **49/49 PASS** 零回归. 总 diff: +49/-422 (净 ~30 行 + 旧 deleted artifacts). SCRIPTS baseline notes: adr_lint.py 报 1 pre-existing 错误 (`adr-0036-three-layer-service-protocol.md` 缺 `## 状态` 章节 — 与本 change 无关, 留作 follow-up); docs_drift_audit.py 报 14 DRIFT items 均与 active ADR (0021/0022/0023/0033 impl-scope-audit) 相关, 0 CRITICAL. OpenSpec change 准备 archive.
- 2026-07-31 (Sprint 14 / C4 ship): C4 `adr-0031-p3p4-toolcoordinator` 实施完成 (3 phase commits: 2d70c87/a48e563). 关键 ship: (1) **ToolCoordinator** — Oracle Option C standalone middleware (ADR-0031 §决策 5, session `ses_0ed4408faffeLv8VfrC0s5PzW7`), 内部流程 layer check → ApprovalHandler → audit invoked → registry.call_tool → audit completed; (2) **LayerProfile 复用** — 既有 enum 配合 ADR-0004 §8 矩阵 (Workflow=all / Thinking=ReadOnly / Cognitive=none), 新增 `parse_layer()`/`check_layer_permission()`/`to_string()` helper; (3) **ToolMetadata V2** — 扩展 `allowed_layers`/`cost_estimate`/`timeout_ms` 3 字段 (默认空/0.0/30000, C4 defer 至 C6 接 IBudgetController + std::async); (4) **Audit Log EventBus** — `tool.audit.{invoked,completed,denied}` topic, payload 塞 `ToolResult::meta` JSON, **args 仅 key 不 value** (defense-in-depth 防 secret 泄露); (5) **NodeExecutor 优先级链** — `tool_coordinator_` > `approval_handler_` > 直接 `call_tool()`, `set_approval_handler()` 标记 `[[deprecated]]` 零回归; (6) **ADR-0031 §决策 4/5/7/8** 新增 — Layer Profile 设计 / ToolCoordinator Option C / Audit Log 命名 / 4 项 defer 至 C6. ADR-0031 P3-P4 🟡 Partial → ✅ Approved. ctest: **41/41 PASS** (39→41, +2 新测试 0 regression). Sprint 11 (C0+C1) + Sprint 12 (C2) + Sprint 13 (C3) + Sprint 14 (C4) 完整 ship. **Sprint 15 (C5 adr-0033-session-hierarchy) 可并行启动** (独立) 或 **Sprint 15 (C7 adr-0034-model-router-plugin) 可启动** (PDK ready).
- 2026-07-31 (Sprint 13 / C3 ship): C3 `adr-0031-p1p2-execution-policy` 实施完成 (6 phase commits: 230120e/acc7f55/76f52f9). 关键 ship: (1) **IExecutionPolicy 5-method 接口** — Oracle 决议落地 (requires_approval/should_execute/can_skip/get_layer/request_approval), 替代 8-method stub; (2) **PlanPolicy/AgentPolicy(默认)/YoloPolicy** — 3 个 5-method 实现 + `PolicyFactory`; (3) **ModeConfig 值结构体** — per-mode 常量移出虚接口; (4) **ApprovalHandler sync callback** — 3 种 transport (stdin/event_bus/test_auto), 不造 EventBus request_id 基础设施 (Oracle 决议); (5) **ModeSwitchDialog** — YOLO 切换需用户确认 (ADR-0031 §决策 6); (6) **DSLEngine 集成** — `set_execution_policy(PolicyMode)`, NodeExecutor 执行前审批检查, 默认 auto-approve 零回归. ADR-0031 🟡 Partial → ✅ Approved. ctest: 39/39 PASS. Sprint 11 (C0+C1) + Sprint 12 (C2) + Sprint 13 (C3) 完整 ship. **Sprint 14 (C4 adr-0031-p3p4-toolcoordinator) 可立即启动**.
- 2026-06-27 (Sprint 12 启动 / C2 ship): C2 `adr-0030-v2-async-runtime` 实施完成 (commits e2fb89b/4b1356a/d474b7d/521ef3f). 关键 ship: (1) **Day 1-2 stream_to_bus bridge runner** — IGenerationStream pull-based → IInteractionBus 事件流 (Oracle Q2 决议, 保持 LLM provider 纯净); (2) **Day 2-3 Context fork/merge** — 深拷贝 + 子覆盖父 (解决 ADR-0030 V2 §风险 🔴 共享可变 Context); (3) **Day 6-8 P2 InMemoryBus EventBus MPMC** — emit() 仅入队 + notify_one, 后台 dispatch_thread 异步分发 (解决 §风险 bridge 背压); (4) **Day 9-10 ADR-0030 V2 文档漂移修正** — §决策记录 + §线程模型 "默认 16" → "默认 4, 可配置 16". C2 ship gate: openspec validate exit 0. test_interaction_bus 1000x 并发在 P2 改后变慢 (异步 dispatch 开销), 留 follow-up. Sprint 11 (C0+C1) + Sprint 12 (C2) 完整 ship, **Sprint 13 (C3) 可立即启动**.
- 2026-06-27 (Sprint 12 启动 / C2 Oracle filled): C2 `adr-0030-v2-async-runtime` Oracle 咨询完成 (session `ses_0f5541ebfffehKDxNVuYqB7bq4`). 2 剩余 Open Questions 决议: (1) **Fleet 模式 16 路 LLM → DEFER** (0 examples 使用并行 LLM, DomainWorkerPool 已提供 N-way 能力, ~1 周节省); (2) **Token 流推送 → bridge runner** `run_stream_to_bus` (IGenerationStream pull-based, Option A 侵入 provider 排除). C2 proposal.md + tasks.md + spec.md 全部 Oracle 决议应用. 估时 2 周 → 1.5-2 周. ADR-0030 V2 §决策记录 line 292 文档漂移待修正 (默认 16 → 默认 4 可配置 16). Sprint 11 ship (C0 + C1) 解锁 C2 启动前置.
- 2026-06-27 (Sprint 11 P0+P1 ship): C1 `sprint-7-tech-debt-execution` 关键工作 ship (35/35 ctest pass, 85/142 tasks done). 关键 ship: (1) **Day 6.2 IBudgetController 抽象** (commit `5aa363c`): 提取 11 纯虚方法接口, factory 返回 `unique_ptr<IBudgetController>`, 减少 engine.cpp 对具体类型依赖; (2) **Day 8-9 scheduler factory** (commit `7125aaf`): 新建 `src/modules/scheduler/factory.{h,cpp}` + 4 测试, engine.cpp 通过 `agenticdsl::scheduler::create()` 工厂构造 + dynamic_cast 移除 (commit `76bf8d2`); (3) **Day 16 IScheduler::get_last_traces()**: 添加到抽象接口, 避免 dynamic_cast. include count 5 (spec 3, 接受为 factory pattern 进化结果).
- 2026-06-26 (Sprint 11 启动 / C0 doc-alignment 收官): OpenSpec change `2026-06-26-doc-alignment-adr-states` ship — 4 处文档/ADR 状态同步: ① 新建 `docs/adr/adr-0030-async-runtime-v2.md` (状态 🔍 Proposed, Phase 2 异步架构, 基于 Slice 00 ship + Sprint 2/3 std::jthread 验证, V2 替代归档的 V1); ② ADR-0030 V1 (`docs/archive/adr/adr-0030-async-runtime-dual-layer.md`) 标记 SUPERSEDED by V2; ③ ADR-0032 (`docs/archive/adr/adr-0032-cost-collector.md`) 状态 ❌ Not Implemented → 🟡 Partial (`tests/test_cost_collector.cpp` 已 ship, 2026-06-14); ④ `docs/archive/implementation-roadmap.md` §Phase 2 ADR 引用 V1 → V2 + ADR-0032 状态更新 + `docs/archive/roadmap-status.md` §Phase 2 行 `⏸ 阻塞中` → `⏸ 待启动 (Sprint 12, 依赖 C0+C1)` + §四 实施日志 2026-06-26 行. C0 ship gate 全部通过 (`adr_lint.py` exit 0, `docs_drift_audit.py` 0 critical drift, `openspec validate` exit 0). OpenSpec change 准备 archive.
- 2026-06-26 (Roadmap-Driven Development 启用): Master plan `docs/superpowers/plans/2026-06-26-sprint-11-to-18-roadmap.md` 创建 (590 行, 含 §9-§13 Review Gates), 9 个 OpenSpec changes 规划 (C0+C1 immediate + C2-C8 placeholder), 2 个 skills 创建 (`master-plan-driven-changes` + `open-spec-placeholder-fill`), 1 个工具创建 (`tools/check_roadmap_drift.py`, 4 类 drift 检测, 当前检测到 2 个 CRITICAL drift 待 C0 修复). Sprint 收官 checklist 加入 Drift Detection 强制项 (§六.6.2), `scripts/sprint-closeout.sh` wrapper 创建。
- 2026-06-26 (Sprint 10 收官): OpenSpec change `2026-06-25-pre-existing-sanitizer-findings` shipped — 2 pre-existing sanitizer 问题全部修复: (1) P1 `test_cognitive_worker` ASan/TSan `stack-use-after-scope` → `std::thread`→`std::jthread` RAII 替换 (commit `d69e2d9`, +20/-10); (2) P2 `test_domain_worker_pool` 12 TSan warnings → Strategy A `std::atomic<bool>` flag post-check 消除 Catch2 framework 数据竞争 (commit `0c44a18`, +34/-13)。Ship gate: ctest 34/34 PASS + ASan 34/34 + TSan 34/34 (0 errors/warnings)。P2.1 调查产出 `docs/audits/p2-tsan-investigation.md` (427 行)。Audit report `docs/audits/2026-06-25-sanitizer-revalidation.md` 追加 §7 Sprint 10 修复后结果。`docs/archive/roadmap-status.md` ASan/TSan 表更新为 34/34 (100%)。OpenSpec change archived。
- 2026-06-25: `2026-06-24-engine-include-final-decoupling` shipped — engine.cpp cross-module includes 10→3 (commits `e7306d9` + `18ce4aa` + `8f2ad54` + `a8abc35` + review fix `a8abc35`), 34/34 ctest pass (新增 15 测试: 7 scheduler `b3ad5bc` + 5 parser `4d1a855` + 3 engine_factory `3681ba8`), pre-existing ASan/TSan findings documented in `docs/archive/roadmap-status.md` (不阻塞 archive), 4-change archive chain closed (`tech-debt-cleanup-sprint-6` → `sprint-9-handle-node-completion` → `2026-06-24-engine-include-final-decoupling` → `tech-debt-and-phase1-closure`), Sprint 10 starts with 0 active OpenSpec changes.
- 2026-06-25 (P2.5 ship gate 复验): `cmake --preset asan -DAGENTICDSL_BUILD_TESTS=ON && ctest` **33/34 PASS** (97%), `cmake --preset tsan -DAGENTICDSL_BUILD_TESTS=ON && ctest` **32/34 PASS** (94%)。2 pre-existing 失败由独立 OpenSpec change `2026-06-25-pre-existing-sanitizer-findings` 跟踪: (1) `test_cognitive_worker` (Sprint 2, `stack-use-after-scope` at test_cognitive_worker.cpp:226, 修复策略: `std::thread` → `std::jthread` C++20 RAII 替换); (2) `test_domain_worker_pool` (Sprint 3, 12 TSan warnings 但 94 assertions 全 PASS, Catch2 framework + std::jthread 已知交互, 产品代码 `DomainWorkerPool` Sprint 3 ship 时 18/18 InMemoryBus 并发断言无 data race 验证已通过, 决策: 文档化非修复)。优雅降级依据: `engine-include-decoupling` spec §"sanitizer-revalidation / 历史 race/leak 优雅降级" Scenario (openspec v1.4.1 per-machine spec registry 维护,非 git-tracked)。Sprint 10 ship gate **PASS** (per pre-existing tracking change 跟踪)。同时完整 ship gate 验证报告 `docs/audits/2026-06-25-sanitizer-revalidation.md` 创建 + `docs/superpowers/plans/2026-06-24-engine-include-final-decoupling.md` plan 文档 commit (895 行, commit `41440c8`) + 2 active 跟踪 plan 目录 README 对账。
- 2026-06-24 (Sprint 5 收官 + 6.3.x 全关闭): OpenSpec change `tech-debt-and-phase1-closure` ship, Phase 1 智能体层 80% → 100%。5 ADR (0019/0020/0021/0022/0023) → ✅ Approved (2026-06-24, Sprint 5 ship)。Sprint 5 S5.T3 phase1_plugin_demo 3 mode CLI (commit `10dc028`)。Sprint 10 起点零 OpenSpec backlog (剩 plugin-loader archive 待执行)。
- 2026-06-22 (Sprint 6 STATUS NOTE 决议): Oracle 深度审查 (session `ses_112a9f9c5ffesqpYeefOBgMkjH`) 决议 — 4 个代码 commit (`7cc4239` / `6c5557c` / `9fa0364` / `7923b2a`) **保持合入不回退** (33/33 ctest pass, 行为保持), **不 archive OpenSpec change**, 偏离 spec 验收项全部推迟到 Sprint 7 follow-up。Top 修复: 🔴 scheduler fork 重复 (`topo_scheduler.cpp:636-642` 死分支) + 🟠 scheduler factory 死代码 (零调用) + 🟠 execute 222 行 vs spec ≤60 + 🟠 engine.cpp 10 include vs spec ≤3 + 🔴 15 个新测试 (7 scheduler + 5 parser + 3 factory) 0 交付。详细偏离表见 `openspec/changes/tech-debt-cleanup-sprint-6/tasks.md` §6.1, Sprint 7 follow-up 见 §6.3。新增 OpenSpec change `2026-07-22-sprint-7-tech-debt-followup` 跟踪。
- 2026-06-21 (commit `fb0e118`): Sprint 6 tech-debt-cleanup OpenSpec change artifacts 提交 (proposal/design/tasks/3 specs)
- 2026-06-20 (commit `7cc4239`): refactor(core): engine.cpp 工厂化, 2/10 跨模块 include 替换 (Sprint 6 P2-7) — **LIMFALL**: 实际计数未降 (10→10), 0 factory 测试, scheduler factory 死代码
- 2026-06-20 (commit `6c5557c`): refactor(parser): introduce NodeFactoryRegistry, eliminate 216行 if-else in create_node_from_json (Sprint 6 P2-6) — **LIMFALL**: 11 NodeType 一一对应零丢失 ✓, 但 0 parser 测试, `has_factory` 预检使 throw 路径成死分支
- 2026-06-20 (commit `9fa0364`): refactor(scheduler): split execute() 308行 → orchestration + 3 subfunctions (Sprint 6 P1-4) — **LIMFALL**: 行为保持 ✓, 但 execute 222 行 vs spec ≤60, 2/3 函数命名不符, fork 处理逻辑被复制两处, 0 scheduler 测试
- 2026-06-20 (commit `7923b2a`): test(plugin): add 7 state-based test cases for PluginLoader (Sprint 6 P1-5) — **LIMFALL**: 7 case 名称/范围与 spec 点名不符, E2E `TEST_PLUGIN_FIXTURE_PATH` 宏未注入, Loaded 状态零覆盖
- 2026-06-14 (commit `451e395`): 修复 3 个失败测试（test_layered_context / test_path_policy / test_secure_tool_registry），全部 25 个测试 100% 通过。核心修复：`flatten` 重命名为 `flatten_layers` 并修复 merge lambda 逻辑、`PathPolicy` 对 `allowed_prefixes` 同样做 `weakly_canonical`、`ShellGuard` 增加 `"| sh"` 模式
- 2026-06-09 (commit `ac9e684`): 删除 `src/modules/prompts.yaml`（LLM prompt 模板改由各模块硬编码或 `llm_config.json` 管理）

<!-- code-review-graph MCP tools -->
## MCP Tools: code-review-graph

**IMPORTANT: This project has a knowledge graph. ALWAYS use the
code-review-graph MCP tools BEFORE using Grep/Glob/Read to explore
the codebase.** The graph is faster, cheaper (fewer tokens), and gives
you structural context (callers, dependents, test coverage) that file
scanning cannot.

### When to use graph tools FIRST

- **Exploring code**: `semantic_search_nodes` or `query_graph` instead of Grep
- **Understanding impact**: `get_impact_radius` instead of manually tracing imports
- **Code review**: `detect_changes` + `get_review_context` instead of reading entire files
- **Finding relationships**: `query_graph` with callers_of/callees_of/imports_of/tests_for
- **Architecture questions**: `get_architecture_overview` + `list_communities`

Fall back to Grep/Glob/Read **only** when the graph doesn't cover what you need.

### Key Tools

| Tool | Use when |
| ------ | ---------- |
| `detect_changes` | Reviewing code changes — gives risk-scored analysis |
| `get_review_context` | Need source snippets for review — token-efficient |
| `get_impact_radius` | Understanding blast radius of a change |
| `get_affected_flows` | Finding which execution paths are impacted |
| `query_graph` | Tracing callers, callees, imports, tests, dependencies |
| `semantic_search_nodes` | Finding functions/classes by name or keyword |
| `get_architecture_overview` | Understanding high-level codebase structure |
| `refactor_tool` | Planning renames, finding dead code |

### Workflow

1. The graph auto-updates on file changes (via hooks).
2. Use `detect_changes` for code review.
3. Use `get_affected_flows` to understand impact.
4. Use `query_graph` pattern="tests_for" to check coverage.
